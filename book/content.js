/* GENERATED BY build.mjs — DO NOT EDIT.
   2 chapters, 18 lessons, 21,881 words.
   Edit the lesson markdown, then re-run: node build.mjs */
window.BOOK = {"title":"System Design","subtitle":"A hands-on course","kicker":"Read it · Run it · Build it","colophon":"A skill counts as learned only when an artifact proves it.","built":"2026-09-01","chapters":[{"id":"api-design","dir":"API Design","title":"API Design","numeral":"I","blurb":"What APIs are, why they are designed the way they are, and how REST, GraphQL and gRPC differ once you stop reading about them and start sending bytes.","lab":"http://localhost:4000","lessons":[{"n":1,"title":"What an API actually is","file":"01-what-is-an-api.md","slug":"what-an-api-actually-is","html":"<div class=\"callout\"><strong>Lab:</strong> open <a class=\"lab-link\" href=\"http://localhost:4000\" target=\"_blank\" rel=\"noopener\">http://localhost:4000</a> → tab <strong>1 · Anatomy</strong>, press <em>Send a real request</em>.<br><strong>Time:</strong> 20 minutes reading, 25 minutes in the lab.</div>\n<h2 id=\"the-one-sentence-definition\">The one-sentence definition</h2>\n<blockquote><p>An API is a <strong>promise about behaviour across a boundary</strong>.</p></blockquote>\n<p>One side promises to accept certain messages and answer in a certain shape. The other side\npromises to ask only in that vocabulary. Everything on the far side of the promise — the\nlanguage, the database, the deploy schedule, the person on call — is private and swappable.</p>\n<p>Most confusion about APIs comes from mistaking the <em>implementation</em> for the <em>interface</em>.\nWhen you call <code>GET /rest/v1/artists/a1</code> in the lab, you learn nothing about how the server\nstores artists. That ignorance is not a limitation. It <strong>is the product</strong>.</p>\n<pre class=\"code\"><code>   ┌──────────────┐        the promise         ┌──────────────────┐\n   │   Client     │ ─────────────────────────▶ │  Implementation  │\n   │              │  URL · method · payload    │                  │\n   │ knows only   │  status codes · errors     │  free to change  │\n   │ the contract │ ◀───────────────────────── │  behind the line │\n   └──────────────┘        the response        └──────────────────┘\n                            ▲\n                 this line is the API.\n                 everything else is detail.</code></pre>\n<h2 id=\"why-promise-and-not-endpoint\">Why \"promise\" and not \"endpoint\"</h2>\n<p>Because the promise is what people build on, and it is bigger than the URL list:</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Part of the promise</th><th>Example</th><th>Break it and…</th></tr></thead><tbody><tr><td><strong>Address</strong></td><td><code>/v1/artists/{id}</code></td><td>every client 404s</td></tr><tr><td><strong>Method semantics</strong></td><td><code>GET</code> is safe and repeatable</td><td>proxies cache a write, or retries duplicate a charge</td></tr><tr><td><strong>Payload shape</strong></td><td><code>{ \"id\": \"a1\", \"name\": \"…\" }</code></td><td>clients crash on a missing field</td></tr><tr><td><strong>Status vocabulary</strong></td><td><code>404</code> means gone, <code>422</code> means your input is wrong</td><td>clients cannot tell \"retry\" from \"give up\"</td></tr><tr><td><strong>Error format</strong></td><td>one shape for every failure</td><td>every integration writes bespoke parsing</td></tr><tr><td><strong>Timing behaviour</strong></td><td>this call answers in &lt;200 ms</td><td>a client's page-load budget silently blows</td></tr><tr><td><strong>Change policy</strong></td><td>additive only, 6-month deprecations</td><td>trust, and then the integration, ends</td></tr></tbody></table></div>\n<p>Anything a consumer can observe and come to depend on is part of your API, whether you meant it\nto be or not. That includes the order of a list you never promised to sort — someone <em>is</em>\ndepending on it right now.</p>\n<h2 id=\"three-kinds-of-api-three-different-jobs\">Three kinds of API, three different jobs</h2>\n<p>The word covers several things. Keep them apart, because their rules differ:</p>\n<ol><li><strong>Library / language APIs</strong> — function signatures in code you import. Coupled at compile time.</li><li><strong>Web / network APIs</strong> — messages crossing a process or network boundary. This course is about these.</li><li><strong>Platform APIs</strong> — what an OS, browser or cloud exposes. Same idea, larger blast radius.</li></ol>\n<p>They share the design principles in Lesson 3. They differ in <em>cost of change</em>: rename a\nprivate function and your compiler tells you; rename a JSON field and you find out from\nsomeone's angry post six weeks later.</p>\n<h2 id=\"what-you-actually-did-in-the-lab\">What you actually did in the lab</h2>\n<p>Pressing <em>Send a real request</em> runs this exchange:</p>\n<pre class=\"code lang-http\"><code>GET /rest/v1/artists/a1 HTTP/1.1\nHost: localhost:4000\nAccept: application/json</code></pre>\n<pre class=\"code lang-http\"><code>HTTP/1.1 200 OK\nContent-Type: application/json; charset=utf-8\nETag: \"0f9c1e6b8a3d2f41\"\nCache-Control: public, max-age=60\n\n{ \"id\": \"a1\", \"type\": \"artist\", \"name\": \"Nova Kim\", … }</code></pre>\n<p>Five design decisions are visible in ten lines, and every one of them was a choice:</p>\n<ul><li><strong><code>GET</code></strong> — safe and idempotent. Any proxy, browser or retry layer may repeat it freely.</li><li><strong><code>/artists/a1</code></strong> — a noun with an address, so it can be linked to, cached and bookmarked.</li><li><strong><code>200</code></strong> — success is in the status line, not buried in the body.</li><li><strong><code>ETag</code></strong> — the response is identified, so the next request can ask \"still this one?\" and get an empty <code>304</code> back. Try it: tick <em>send If-None-Match</em> on the REST tab.</li><li><strong><code>Cache-Control</code></strong> — the server states how long the answer stays true. That single header can remove more load than a week of query optimisation.</li></ul>\n<h2 id=\"the-mental-model-to-keep\">The mental model to keep</h2>\n<p>An API is a <strong>product with users</strong> — even an internal one whose users sit ten feet away.\nThat reframing predicts most of the good advice in this course:</p>\n<ul><li>Products get <strong>documentation</strong> and examples that run.</li><li>Products get <strong>versions</strong> and deprecation notices, not silent changes.</li><li>Products are designed around <strong>what users are trying to do</strong>, not around your storage.</li><li>Products have <strong>support costs</strong>: every ambiguity in the contract becomes a message to you.</li></ul>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Define an API without using the word \"endpoint\".</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;List what is inside a contract beyond the URL and the JSON fields.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why hiding the implementation is the feature, not a side effect.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Point at a response in the lab and name the design decision behind each header.</li></ul>\n<p><strong>Artifact for this module:</strong> pick an API you use every day (Stripe, GitHub, your own team's).\nWrite one page: what it promises, what it hides, and what would break tomorrow if its owners\nrenamed one field. Keep it — Lesson 3 will ask you to grade it.</p>\n<p>Next: <a href=\"#/api-design/2\">Lesson 2 — APIs in system architecture</a></p>","words":862,"minutes":4,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Define an API without using the word \"endpoint\".</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;List what is inside a contract beyond the URL and the JSON fields.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why hiding the implementation is the feature, not a side effect.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Point at a response in the lab and name the design decision behind each header.</li></ul>"},{"n":2,"title":"APIs in system architecture","file":"02-apis-in-system-architecture.md","slug":"apis-in-system-architecture","html":"<div class=\"callout\"><strong>Lab:</strong> tab <strong>2 · Architecture</strong> — click every box in the diagram.<br><strong>Time:</strong> 25 minutes reading, 35 minutes mapping your own system.</div>\n<h2 id=\"apis-are-where-a-system-s-seams-are\">APIs are where a system's seams are</h2>\n<p>Architecture is mostly the question <em>where do we cut?</em> An API is a cut you have committed to.\nDraw any system and the arrows crossing box boundaries are its APIs — and the boxes are only\nindependent to the degree those arrows are well-designed.</p>\n<pre class=\"code\"><code>   Web app        Mobile app        Partner systems\n      │                │                   │\n      └────────────────┼───────────────────┘\n                       ▼\n        ┌──────────────────────────────────┐\n        │   API gateway / edge             │  auth · rate limits · TLS\n        │                                  │  routing · versioning · logs\n        └──────────────────────────────────┘\n             │              │            │\n       ┌─────▼────┐  ┌──────▼─────┐  ┌───▼──────┐\n       │ Catalog  │  │ Playback   │  │ Billing  │\n       │ REST     │◀▶│ gRPC       │◀▶│ gRPC     │\n       └─────┬────┘  └──────┬─────┘  └───┬──────┘\n             ▼              ▼            ▼\n         Postgres      event stream    Stripe API\n                                    (someone else's contract)</code></pre>\n<p>Three things worth staring at:</p>\n<ol><li><strong>The protocol changes at the edge.</strong> Public traffic arrives as REST or GraphQL over HTTPS; internal hops are gRPC or events. That is not fashion — it is each side optimising for a different consumer (Lesson 9).</li><li><strong>Cross-cutting concerns live in one place.</strong> Auth, quotas, TLS, request logging, version routing. Spread them across services and you will get eight subtly different implementations and one incident.</li><li><strong>The bottom-right box.</strong> Stripe's API is <em>someone else's</em> contract, and you are the client with hard-coded assumptions. Every rule in this course will one day be applied to you by a stranger reading your docs at 2 a.m.</li></ol>\n<h2 id=\"three-audiences-three-rule-sets\">Three audiences, three rule sets</h2>\n<div class=\"tablewrap\"><table><thead><tr><th></th><th><strong>Public</strong></th><th><strong>Partner</strong></th><th><strong>Internal</strong></th></tr></thead><tbody><tr><td>Consumer</td><td>strangers</td><td>named organisations</td><td>your own teams</td></tr><tr><td>Discovery</td><td>docs, SDKs, search</td><td>onboarding, sandbox</td><td>the repo</td></tr><tr><td>Cost of a breaking change</td><td>permanent; you cannot call them</td><td>contractual, sometimes legal</td><td>one coordinated deploy</td></tr><tr><td>Versioning</td><td>strict, long deprecations</td><td>negotiated with dates</td><td>often none needed</td></tr><tr><td>Typical protocol</td><td>REST (+ webhooks)</td><td>REST + webhooks</td><td>gRPC or events</td></tr><tr><td>Optimise for</td><td>clarity and stability</td><td>predictability</td><td>latency and type safety</td></tr></tbody></table></div>\n<p>The single most common architecture mistake is applying internal rules to a public API\n(\"we'll just change it, everyone deploys together\") or public rules to an internal one\n(three versions of a service ten people use, all maintained forever).</p>\n<h2 id=\"the-api-is-where-coupling-becomes-visible\">The API is where coupling becomes visible</h2>\n<p>Coupling never disappears; it moves. An API decides <em>which kind</em> you get:</p>\n<ul><li><strong>Shape coupling</strong> — the client depends on your response fields. Fix: expose what consumers need, not your table columns; add fields, never repurpose them.</li><li><strong>Temporal coupling</strong> — the caller blocks while you work. Fix: make long jobs asynchronous — accept with <code>202</code>, hand back a status resource, or publish an event.</li><li><strong>Availability coupling</strong> — your outage becomes their outage. Fix: timeouts, retries with backoff and jitter, circuit breakers, and a degraded answer where one is honest.</li><li><strong>Deployment coupling</strong> — both sides must ship together. Acceptable internally, fatal publicly. This is what versioning buys you.</li></ul>\n<p>A useful review question: *if this dependency's owners disappeared for a month, what happens\nto us?* The answer is a property of the API, not of their team.</p>\n<h2 id=\"synchronous-request-response-is-not-the-only-shape\">Synchronous request/response is not the only shape</h2>\n<p>This course focuses on request/response (REST, GraphQL, gRPC) because that is where design\nskill transfers. But part of designing an API is noticing when it should not be a call at all:</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Shape</th><th>Use when</th><th>Example here</th></tr></thead><tbody><tr><td>Request/response</td><td>the caller needs the answer to continue</td><td><code>GET /artists/a1</code></td></tr><tr><td>Server streaming</td><td>results arrive over time</td><td>gRPC <code>WatchPlays</code> in Lab 3</td></tr><tr><td>Webhooks</td><td><em>you</em> must tell <em>them</em>, later</td><td>\"payment settled\"</td></tr><tr><td>Events / queue</td><td>many consumers, none blocking</td><td>\"track played\" → analytics, royalties, recommendations</td></tr></tbody></table></div>\n<p>Rule of thumb: <strong>if the caller does not need the result to continue, do not make them wait\nfor it.</strong> Half of all latency problems are a synchronous call that had no business being one.</p>\n<h2 id=\"conway-s-law-briefly\">Conway's law, briefly</h2>\n<p>Teams ship their communication structure as architecture. If two teams own one service, its\nAPI will grow two dialects. If one team owns three services, their \"APIs\" will quietly become\nfunction calls with network latency attached. When you choose service boundaries, you are\nchoosing team boundaries — and vice versa. Design the seam where the org can actually hold it.</p>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Sketch a system as boxes and arrows and name every arrow as an API.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say which of your APIs are public, partner or internal — and apply different rules to each.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name the four kinds of coupling and one mitigation for each.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Spot a synchronous call that should be an event.</li></ul>\n<p><strong>Artifact for this module:</strong> map a system you actually work on (or the last one you used).\nBoxes for consumers, edge, services, stores. Label each arrow with its protocol, its audience,\nand a 1–5 score for <em>cost of changing it</em>. The 5s are where the rest of this course pays off.</p>\n<p>Next: <a href=\"#/api-design/3\">Lesson 3 — Principles of good API design</a></p>","words":886,"minutes":4,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Sketch a system as boxes and arrows and name every arrow as an API.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say which of your APIs are public, partner or internal — and apply different rules to each.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name the four kinds of coupling and one mitigation for each.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Spot a synchronous call that should be an event.</li></ul>"},{"n":3,"title":"The principles that make an API good","file":"03-design-principles.md","slug":"the-principles-that-make-an-api-good","html":"<div class=\"callout\"><strong>Lab:</strong> tab <strong>3 · REST</strong>. Click through <em>Endpoints</em>, then <em>Try the mechanics</em>, and finish<br>with the red <strong>the BAD design</strong> button. Compare them side by side.<br><strong>Time:</strong> 35 minutes reading, 25 minutes in the lab.</div>\n<p>These ten hold across REST, GraphQL and gRPC. Protocol choice changes <em>how</em> you honour them,\nnever <em>whether</em> you should.</p>\n<h2 id=\"1-design-for-the-consumer-s-job-not-your-schema\">1. Design for the consumer's job, not your schema</h2>\n<p>The first draft of most APIs is a database viewer: one endpoint per table, every column\nexposed. It ships fast and hurts forever, because your tables change for reasons your\nconsumers cannot see, and their screens need joins you did not anticipate.</p>\n<p>Start from sentences: <em>\"show an artist page\"</em>, <em>\"let a partner sync yesterday's plays\"</em>.\nThen design the smallest contract that serves them.</p>\n<blockquote><p><strong>Test:</strong> can you rename a database column without a client noticing? If not, you published\nyour schema, not an API.</p></blockquote>\n<h2 id=\"2-consistency-beats-cleverness\">2. Consistency beats cleverness</h2>\n<p>Every inconsistency is a thing to remember, and memory is the scarce resource in an\nintegration. Pick one answer for each of these and never deviate:</p>\n<ul><li>casing (<code>snake_case</code> or <code>camelCase</code> — pick one, apply everywhere)</li><li>dates and times (ISO 8601, UTC, always; <code>2026-08-31T12:43:58Z</code>)</li><li>money (minor units as integers + currency code; never floats)</li><li>IDs (opaque strings; not auto-increment integers that leak volume and invite enumeration)</li><li>collection envelopes, pagination parameters, sort syntax, error shape</li></ul>\n<p>An API where a developer can <em>guess</em> the next endpoint correctly is the goal. In the lab,\n<code>/artists/a1/albums</code> and <code>/albums/b1/tracks</code> follow one rule; you could have guessed the second\nafter seeing the first.</p>\n<h2 id=\"3-names-are-the-interface\">3. Names are the interface</h2>\n<p>You will rewrite the implementation three times and the vocabulary never. Use the words the\nbusiness already uses. Avoid <code>data</code>, <code>info</code>, <code>item</code>, <code>object</code>, <code>process</code>, <code>handle</code>, and any\nname that describes your framework rather than the domain. If two names compete, ask which one\na new hire would say out loud.</p>\n<h2 id=\"4-make-the-common-case-easy-and-the-whole-surface-small\">4. Make the common case easy and the whole surface small</h2>\n<p>Every endpoint, field and option is a lifetime commitment: documented, tested, secured,\nmigrated. Ship the smallest surface that does the job; add on evidence, not speculation.\nIt is trivial to add a field later and near-impossible to remove one.</p>\n<h2 id=\"5-errors-are-a-feature-and-they-need-one-shape\">5. Errors are a feature, and they need one shape</h2>\n<p>Errors are the part of your API that integrators handle most and you test least. Give them:</p>\n<ul><li><strong>the right status</strong> — <code>400</code> malformed, <code>401</code> unauthenticated, <code>403</code> unauthorised, <code>404</code> absent, <code>409</code> conflict, <code>422</code> well-formed but invalid, <code>429</code> rate-limited, <code>5xx</code> our fault</li><li><strong>one body shape everywhere</strong> — the lab uses RFC 9457 <code>application/problem+json</code></li><li><strong>actionable detail</strong> — which field, what was wrong, what to do</li><li><strong>a retry signal</strong> — is this permanent, or should the client back off and try again?</li><li><strong>no leaks</strong> — no stack traces, SQL, or internal hostnames</li></ul>\n<pre class=\"code lang-jsonc\"><code>// GET /rest/v1/artists  (POST with a bad body)  → 422\n{\n  \"type\": \"https://riff.example/errors/validation-failed\",\n  \"title\": \"Validation failed\",\n  \"status\": 422,\n  \"detail\": \"One or more fields were rejected.\",\n  \"errors\": [\n    { \"field\": \"name\",    \"issue\": \"required\" },\n    { \"field\": \"country\", \"issue\": \"must be an ISO 3166-1 alpha-2 code\" }\n  ]\n}</code></pre>\n<p>Compare with the red button in the lab, which returns <strong><code>200 OK</code></strong> carrying\n<code>{\"status\":\"error\"}</code>. Every proxy, retry layer, dashboard and alert in the world now believes\nthat request succeeded. That is the bug — not the wording.</p>\n<h2 id=\"6-safety-idempotency-and-retries\">6. Safety, idempotency and retries</h2>\n<p>The network will deliver your request twice. Design for it:</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Property</th><th>Meaning</th><th>Methods</th></tr></thead><tbody><tr><td><strong>Safe</strong></td><td>changes nothing</td><td><code>GET</code>, <code>HEAD</code>, <code>OPTIONS</code></td></tr><tr><td><strong>Idempotent</strong></td><td>doing it twice ≡ doing it once</td><td><code>GET</code>, <code>PUT</code>, <code>DELETE</code>, <code>PATCH</code> (if written that way)</td></tr><tr><td>Neither</td><td>each call is a new effect</td><td><code>POST</code></td></tr></tbody></table></div>\n<p>For unsafe operations that must survive a retry, accept an <strong>idempotency key</strong> and return the\nfirst result on a repeat. Try the <em>idempotent retry</em> button twice in the lab: the second call\nreturns <code>200</code> with <code>Idempotent-Replay: true</code> instead of creating a duplicate artist. Payments\nAPIs live or die on this.</p>\n<h2 id=\"7-pagination-filtering-and-limits-from-day-one\">7. Pagination, filtering and limits — from day one</h2>\n<p>Any collection that can grow must be paginated at launch. Retrofitting pagination is a\nbreaking change, and unbounded lists are how a Tuesday becomes an incident.</p>\n<ul><li>Prefer <strong>cursor</strong> pagination over <code>offset</code>/<code>limit</code>: stable under inserts, cheap on large tables.</li><li>Make the cursor <strong>opaque</strong> (the lab base64s the last id) so clients cannot do arithmetic on it and you stay free to change the scheme.</li><li>Cap <code>limit</code> server-side. The lab caps at 50 regardless of what you ask for.</li><li>Same for cost: rate limits, payload size limits, query depth limits (Lesson 7).</li></ul>\n<h2 id=\"8-evolve-additively-version-only-when-you-must\">8. Evolve additively; version only when you must</h2>\n<p>Non-breaking, always allowed: adding an endpoint, adding an optional field, adding an enum\nvalue <em>if clients were told to tolerate unknowns</em>, relaxing a validation rule.</p>\n<p>Breaking: removing or renaming anything, changing a type, making an optional field required,\ntightening validation, changing defaults, changing the meaning of an existing field.</p>\n<p>When you must break: a new version (<code>/v2</code>), both live at once, a deprecation date in writing,\n<code>Deprecation</code>/<code>Sunset</code> headers, and usage metrics per consumer so you know who is still on v1.\n\"We'll email everyone\" is not a migration plan.</p>\n<h2 id=\"9-secure-at-the-resource-not-at-the-route\">9. Secure at the resource, not at the route</h2>\n<p>Authentication answers <em>who is this</em>; authorisation answers <em>may they touch this object</em>.\nThe classic breach is a correct login plus an unchecked <code>id</code> — change <code>a1</code> to <code>a2</code> and read\nsomeone else's data. Check ownership on every object you return or mutate. Also:</p>\n<ul><li>validate input against the schema at the boundary, and reject unknown fields loudly</li><li>never accept a client-supplied <code>role</code>, <code>price</code> or <code>userId</code> for a privileged decision</li><li>rate-limit per credential, and return <code>429</code> with <code>Retry-After</code></li><li>log who did what, and never log the credential itself</li></ul>\n<h2 id=\"10-documentation-and-examples-are-part-of-the-product\">10. Documentation and examples are part of the product</h2>\n<p>Undocumented behaviour gets discovered, depended on and then locked in anyway — but wrongly.\nShip a machine-readable spec (OpenAPI, SDL, <code>.proto</code>), a quickstart that works when pasted,\nrunnable examples per endpoint, error catalogue, limits, and a changelog. Generate the\nreference from the spec so it cannot drift.</p>\n<hr>\n<h2 id=\"grading-the-lab-s-own-api\">Grading the lab's own API</h2>\n<div class=\"tablewrap\"><table><thead><tr><th>Principle</th><th><code>/rest/v1/...</code> (good)</th><th><code>/rest/bad/getArtistData?id=a1</code></th></tr></thead><tbody><tr><td>Consumer-shaped</td><td>resources map to screens</td><td>dumps every field, always</td></tr><tr><td>Naming</td><td>nouns + HTTP verbs</td><td>verb in the URL, <code>data</code>, <code>info</code></td></tr><tr><td>Errors</td><td><code>404</code> + problem+json</td><td><code>200 OK</code> with an error string</td></tr><tr><td>Caching</td><td><code>ETag</code> + <code>Cache-Control</code> → <code>304</code></td><td>none; every call is full price</td></tr><tr><td>Least surface</td><td><code>?fields=</code> when you want less</td><td>you get everything, forever</td></tr></tbody></table></div>\n<p>Run both and watch the <strong>bytes</strong> and <strong>datastore reads</strong> counters. The bad endpoint is not\nmerely ugly; it is measurably more expensive on every single call.</p>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Recite the ten principles and give one concrete violation you have personally met.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Write an error response that a client can act on without reading your source.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain idempotency to someone who thinks retries are free.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say which changes to a contract are safe and which need a version.</li></ul>\n<p><strong>Artifact for this module:</strong> take the API you wrote up in Lesson 1 and grade it 0–2 against\neach principle. For every score below 2, write the fix and what it buys. That document is a\ndesign review — and it is the exact skill this course is for.</p>\n<p>Next: <a href=\"#/api-design/4\">Lesson 4 — How protocols shape API design</a></p>","words":1280,"minutes":6,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Recite the ten principles and give one concrete violation you have personally met.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Write an error response that a client can act on without reading your source.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain idempotency to someone who thinks retries are free.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say which changes to a contract are safe and which need a version.</li></ul>"},{"n":4,"title":"How application protocols shape API design","file":"04-protocols-shape-design.md","slug":"how-application-protocols-shape-api-design","html":"<div class=\"callout\"><strong>Lab:</strong> tabs <strong>3 · REST</strong>, <strong>4 · GraphQL</strong>, <strong>5 · gRPC</strong>. Watch the <em>Response headers</em> panel<br>and the byte counters. Then tab <strong>6 · Compare</strong> and run the benchmark.<br><strong>Time:</strong> 30 minutes reading, 30 minutes measuring.</div>\n<h2 id=\"the-claim\">The claim</h2>\n<p>Your protocol is not a delivery detail you pick after designing the API. It decides <strong>which\ndesign ideas are cheap, which are expensive, and which are impossible</strong>. Every style in this\ncourse is really \"an API design idea, plus the protocol that makes it affordable\".</p>\n<h2 id=\"layers-quickly\">Layers, quickly</h2>\n<pre class=\"code\"><code>your API design        resources / a type graph / procedures\n─────────────────────────────────────────────────────────────\napplication protocol   HTTP/1.1 · HTTP/2 · HTTP/3 · WebSocket\n─────────────────────────────────────────────────────────────\ntransport              TCP (or QUIC over UDP for HTTP/3)</code></pre>\n<p>REST leans hard on the application protocol. GraphQL mostly ignores it. gRPC depends on a\nspecific version of it. That single sentence explains most of their differences.</p>\n<h2 id=\"what-http-gives-you-for-free-if-your-design-lets-it\">What HTTP gives you for free — if your design lets it</h2>\n<div class=\"tablewrap\"><table><thead><tr><th>HTTP feature</th><th>The design decision that unlocks it</th><th>You lose it when…</th></tr></thead><tbody><tr><td><strong>Methods</strong> (<code>GET</code>/<code>POST</code>/<code>PUT</code>/<code>PATCH</code>/<code>DELETE</code>)</td><td>put the verb in the method, the noun in the URL</td><td>everything is <code>POST /graphql</code></td></tr><tr><td><strong>Status codes</strong></td><td>let the status line carry the outcome</td><td>you return <code>200</code> with an error body</td></tr><tr><td><strong>Caching</strong> (<code>ETag</code>, <code>Cache-Control</code>, <code>304</code>)</td><td>give each thing a stable URL</td><td>one URL serves every possible response</td></tr><tr><td><strong>Conditional requests</strong> (<code>If-None-Match</code>)</td><td>identify representations</td><td>responses are query-shaped, not resource-shaped</td></tr><tr><td><strong>Content negotiation</strong> (<code>Accept</code>)</td><td>separate resource from representation</td><td>the format is baked into the path</td></tr><tr><td><strong>Range requests</strong></td><td>expose large blobs as resources</td><td>you stream through a custom endpoint</td></tr><tr><td><strong>Auth, proxies, CDNs, browsers, curl</strong></td><td>speak plain HTTP</td><td>you invent a framing on top</td></tr></tbody></table></div>\n<p>Seen live in the lab: click <em>collection, paginated</em>, tick <strong>send If-None-Match</strong>, click again.\nThe second response is <strong><code>304 Not Modified</code>, zero body bytes</strong>. That is the whole of your\ncaching layer for one line of server code — and it exists only because the URL identifies one\nstable thing.</p>\n<h2 id=\"http-1-1-vs-http-2-the-constraint-that-created-two-design-st\">HTTP/1.1 vs HTTP/2 — the constraint that created two design styles</h2>\n<p><strong>HTTP/1.1</strong> sends one request at a time per connection. Browsers open ~6 connections per host;\nafter that, requests queue. So on HTTP/1.1 <strong>round trips are the scarce resource</strong>, and API\ndesign pays for chattiness. That pressure produced compound documents, <code>?expand=</code>, and\neventually GraphQL.</p>\n<p><strong>HTTP/2</strong> multiplexes many streams over one connection, compresses headers (HPACK), and\nsupports long-lived bidirectional streams. Round trips get much cheaper — though not free:\nlatency is still latency, and a dependent chain of calls (fetch A to learn B's id) still\nserialises. gRPC <em>requires</em> HTTP/2, which is exactly why it can offer four streaming modes\nwith no bolt-ons.</p>\n<p><strong>HTTP/3</strong> moves to QUIC over UDP: no TCP head-of-line blocking, faster handshakes. Mostly\nfree performance for the same API design.</p>\n<p>Numbers from a real run of the lab's benchmark (\"render one artist page\"):</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Style</th><th>Round trips</th><th>Bytes</th></tr></thead><tbody><tr><td>REST (naive)</td><td>4</td><td>2,568</td></tr><tr><td>REST (<code>?expand=tracks</code>)</td><td>4</td><td>3,106</td></tr><tr><td>GraphQL</td><td>1</td><td>795</td></tr><tr><td>gRPC</td><td>1</td><td>199 (protobuf; 523 as JSON)</td></tr></tbody></table></div>\n<p>Read that table twice, because the obvious conclusion is wrong. On localhost a round trip\ncosts ~1 ms and the byte counts hardly matter. On a mobile network each round trip is\n80–150 ms and those four REST calls are half a second before the server does any work. The\nsame table on a LAN between two services says something different again — there, the 199 bytes\nand the absence of JSON parsing matter more than the trip count.</p>\n<p><strong>The protocol does not tell you what is fast. Your network and your consumer do.</strong></p>\n<h2 id=\"serialisation-json-vs-protobuf\">Serialisation: JSON vs protobuf</h2>\n<div class=\"tablewrap\"><table><thead><tr><th></th><th>JSON</th><th>Protobuf</th></tr></thead><tbody><tr><td>Readable by a human</td><td>yes</td><td>no, you need the <code>.proto</code></td></tr><tr><td>Self-describing</td><td>yes (field names on the wire)</td><td>no (field <em>numbers</em> on the wire)</td></tr><tr><td>Size</td><td>baseline</td><td>typically 30–70 % smaller</td></tr><tr><td>Parse cost</td><td>high (string scanning)</td><td>low (binary, generated code)</td></tr><tr><td>Schema</td><td>optional, often drifts</td><td>mandatory, compiled</td></tr><tr><td>Evolution</td><td>ad hoc</td><td>rules built in (reserved tags, optional fields)</td></tr></tbody></table></div>\n<p>The lab shows the same <code>ListAlbums</code> payload as <strong>199 protobuf bytes vs 523 JSON bytes</strong> — 62 %\nsmaller. At ten requests a second nobody cares. At a hundred thousand, that is a hardware line\nitem and a p99 improvement.</p>\n<p>Note the trade you make with a binary format: you can no longer debug with <code>curl</code> and your\neyes, and no browser can read it without a gateway. Legibility is a real feature; it is just\nnot always the most valuable one.</p>\n<h2 id=\"streaming-and-what-each-protocol-makes-possible\">Streaming, and what each protocol makes possible</h2>\n<div class=\"tablewrap\"><table><thead><tr><th>Need</th><th>HTTP/1.1</th><th>HTTP/2</th><th>gRPC (on HTTP/2)</th></tr></thead><tbody><tr><td>Server pushes updates</td><td>polling, long-polling, SSE</td><td>SSE</td><td>server streaming, native</td></tr><tr><td>Client uploads a stream</td><td>chunked upload</td><td>chunked</td><td>client streaming, native</td></tr><tr><td>Both directions</td><td>WebSocket (separate protocol)</td><td>WebSocket</td><td>bidirectional streaming, native</td></tr></tbody></table></div>\n<p>Lab 3's <code>WatchPlays</code> is one request answered by many messages on one connection. The bridge\nre-emits it to your browser as SSE — because <strong>the browser cannot speak gRPC</strong>. That hop\n(Envoy or gRPC-Web in production) is the honest cost of choosing gRPC at the edge, and it is\nwhy most systems keep gRPC internal.</p>\n<h2 id=\"statelessness-and-why-it-is-a-protocol-shaped-decision\">Statelessness and why it is a protocol-shaped decision</h2>\n<p>HTTP is stateless: each request carries everything needed to serve it. That is what lets any\nof ten servers answer, lets you scale horizontally, and lets a CDN answer for you. Designs that\nsmuggle in state — server-side sessions keyed by cookie, a cursor that means \"the next page of\nwhatever you asked last time\" — trade that away for convenience. Sometimes worth it. Never free.</p>\n<h2 id=\"the-transferable-rule\">The transferable rule</h2>\n<p>When you evaluate any protocol, ask these five questions:</p>\n<ol><li><strong>Who can speak it?</strong> (browser? mobile? a partner's 2009 SOAP stack?)</li><li><strong>What does it give me for free?</strong> (caching, retries, status semantics, streaming)</li><li><strong>What does it cost per message?</strong> (bytes, parse time, connection setup)</li><li><strong>What does it make impossible?</strong> (browser-native gRPC; HTTP caching for GraphQL)</li><li><strong>What operational tooling exists?</strong> (curl, proxies, CDNs, dashboards, tracing)</li></ol>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why REST gets caching \"free\" and GraphQL does not.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what HTTP/2 changed and why gRPC requires it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Quote your <em>own</em> measured numbers for round trips and bytes across the three styles.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Argue against \"protocol X is faster\" using the round-trips-vs-bytes distinction.</li></ul>\n<p><strong>Artifact for this module:</strong> run the benchmark, record your numbers, and write half a page:\nwhich style wins for a mobile client on 4G, which for a service-to-service call on the same\nLAN, and why the answer differs. Include the caveat about localhost measurements.</p>\n<p>Next: <a href=\"#/api-design/5\">Lesson 5 — The API design process</a></p>","words":1189,"minutes":6,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why REST gets caching \"free\" and GraphQL does not.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what HTTP/2 changed and why gRPC requires it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Quote your <em>own</em> measured numbers for round trips and bytes across the three styles.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Argue against \"protocol X is faster\" using the round-trips-vs-bytes distinction.</li></ul>"},{"n":5,"title":"The API design process","file":"05-the-design-process.md","slug":"the-api-design-process","html":"<div class=\"callout\"><strong>Lab:</strong> tab <strong>7 · Design process</strong> — the seven steps with a checklist that saves your progress.<br><strong>Worksheet:</strong> <span class=\"offbook\"><code>worksheets/api-design-canvas.md</code></span><br><strong>Time:</strong> 30 minutes reading, 90 minutes doing it for real on a system you care about.</div>\n<p>Design happens <strong>before</strong> the first route handler. The order below is not bureaucracy; each\nstep is cheap where it sits and brutally expensive one step later.</p>\n<pre class=\"code\"><code>1 jobs ──▶ 2 domain ──▶ 3 style ──▶ 4 contract ──▶ 5 prototype ──▶ 6 review ──▶ 7 ship\n                                       ▲                 │\n                                       └── rewrite here ─┘   ← the cheapest edit you will make</code></pre>\n<hr>\n<h2 id=\"step-1-find-the-jobs-not-the-tables\">Step 1 · Find the jobs, not the tables</h2>\n<p>Write down what each consumer is trying to accomplish, in their words, as sentences:</p>\n<ul><li>\"Show an artist page with their albums and track lengths.\"</li><li>\"Let a partner sync yesterday's plays every morning.\"</li><li>\"Let the mobile app work when the tube goes into a tunnel.\"</li></ul>\n<p>For each, note the actor, the trigger, the data they need, and their constraints (slow network?\nno proxy? cannot ship an update for six weeks?).</p>\n<p><strong>Output:</strong> 5–10 job sentences with the actor named.\n<strong>Smell:</strong> your first draft mirrors your database. You have designed a database viewer.</p>\n<h2 id=\"step-2-model-the-domain\">Step 2 · Model the domain</h2>\n<p>Name the nouns, their relationships, and their identity. This vocabulary outlives every\nimplementation detail you will ever write.</p>\n<ul><li>Use the words the business already says out loud. <code>artist</code>, not <code>content_entity</code>.</li><li>Decide identity: opaque IDs (<code>a1</code>, <code>trk_9f2…</code>), not database row numbers — those leak volume, invite enumeration and pin you to one storage engine.</li><li>Mark cardinality: an artist has many albums; an album has one artist.</li><li>Decide what is a resource and what is a field. If it has its own lifecycle, its own permissions, or its own URL in a UI, it is probably a resource.</li></ul>\n<p><strong>Output:</strong> an entity sketch with IDs, relationships and cardinality.</p>\n<h2 id=\"step-3-choose-the-style-deliberately\">Step 3 · Choose the style deliberately</h2>\n<p>REST, GraphQL, gRPC, events — pick with the decision flow (lab tab 6, or\n<span class=\"offbook\"><code>worksheets/protocol-decision-worksheet.md</code></span>)\nand <strong>write down the reason</strong>. \"It is what the team knows\" is a legitimate reason. An\nunrecorded one is not, because in a year nobody will remember the constraint that drove it.</p>\n<p><strong>Output:</strong> one defensible paragraph, plus a note on what would change your mind.</p>\n<h2 id=\"step-4-design-the-contract-before-the-implementation\">Step 4 · Design the contract — before the implementation</h2>\n<p>Write the spec file first: OpenAPI, GraphQL SDL, or <code>.proto</code>. Writing it forces every decision\ninto the open while it is still free to change.</p>\n<p>Decide once, apply everywhere:</p>\n<ul><li>resource or type names, casing, plurals</li><li>every payload shape, <strong>including errors</strong></li><li>pagination, filtering, sorting conventions</li><li>auth: who calls this, with what credential, at what rate</li><li>what is required vs optional, and every default</li><li>idempotency for unsafe operations</li></ul>\n<p><strong>Output:</strong> a spec file that exists before any handler. In this course, <code>labs/grpc/catalog.proto</code>\nand the SDL in <code>labs/graphql/schema.js</code> are exactly this artifact.</p>\n<h2 id=\"step-5-prototype-against-the-contract\">Step 5 · Prototype against the contract</h2>\n<p>Generate a mock from the spec and have a real consumer build a real screen against it. You\nwill discover the missing field on day one — when it costs a text edit — instead of after\nlaunch, when it costs a version.</p>\n<ul><li>Prism or <code>msw</code> for OpenAPI; any GraphQL server can serve a schema with mocked resolvers.</li><li>Give the mock to the person who will actually integrate. Watch them, do not brief them.</li><li>Every question they ask is a documentation gap or a naming failure. Write both down.</li></ul>\n<p><strong>Output:</strong> a working client screen fed by a mock, and a list of contract edits.</p>\n<h2 id=\"step-6-review-it-like-a-publication\">Step 6 · Review it like a publication</h2>\n<p>Use <span class=\"offbook\"><code>worksheets/design-review-checklist.md</code></span>. The\nquestions that catch the most:</p>\n<ul><li>What happens on a <strong>retry</strong>? On a <strong>partial failure</strong>? On a <strong>client three versions old</strong>?</li><li>Can a caller reach another tenant's object by changing an id?</li><li>Is every collection paginated and every limit capped?</li><li>Is there exactly one error shape, and does it say what to do next?</li><li>Is every unsafe operation idempotent, or is it documented why not?</li><li>Is the versioning and deprecation policy written down, with dates?</li></ul>\n<p><strong>Output:</strong> a signed-off contract plus a written change policy.</p>\n<h2 id=\"step-7-ship-with-the-operational-parts-attached\">Step 7 · Ship with the operational parts attached</h2>\n<p>An API without docs, limits and metrics is a liability that happens to return JSON.</p>\n<ul><li>Reference docs generated from the spec, plus a quickstart that works when pasted.</li><li>Rate limits and quotas: documented, enforced, and returning <code>429</code> with <code>Retry-After</code>.</li><li>Metrics per endpoint: latency percentiles, error rate, usage <strong>per consumer</strong> — you cannot deprecate what you cannot measure.</li><li>A changelog, and a way to warn consumers before you break them.</li></ul>\n<p><strong>Output:</strong> docs with runnable examples, dashboards, limits, changelog.</p>\n<hr>\n<h2 id=\"how-this-looks-compressed-for-a-small-internal-api\">How this looks compressed for a small internal API</h2>\n<p>You do not need a two-week process for an endpoint two teams use. The compressed version, in\nabout an hour:</p>\n<ol><li>Three job sentences in the ticket.</li><li>The nouns, in a comment.</li><li>\"gRPC, because it is internal and high-volume\" — one line in the PR description.</li><li>The <code>.proto</code> in the first commit, reviewed on its own before any implementation.</li><li>The consuming team reviews the <code>.proto</code>, not the code.</li><li>Checklist run over the diff.</li><li>Docstrings in the proto + a dashboard panel.</li></ol>\n<p>Same seven steps. The point is the <em>order</em>, not the ceremony.</p>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Run the seven steps on a real feature without looking them up.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why the spec is written before the handler.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what each step produces and what it costs to skip.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Compress the process honestly for a small internal API.</li></ul>\n<p><strong>Artifact for this module:</strong> a completed\n<span class=\"offbook\">API Design Canvas</span> for a system you care about — jobs,\nentities, chosen style with rationale, draft contract, error shape, limits, change policy.\nThis is the single most reusable thing you will make in this course.</p>\n<p>Next: <a href=\"#/api-design/6\">Lesson 6 — REST in depth</a></p>","words":1016,"minutes":5,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Run the seven steps on a real feature without looking them up.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why the spec is written before the handler.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what each step produces and what it costs to skip.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Compress the process honestly for a small internal API.</li></ul>"},{"n":6,"title":"REST in depth","file":"06-rest.md","slug":"rest-in-depth","html":"<div class=\"callout\"><strong>Lab:</strong> tab <strong>3 · REST</strong>. <strong>Source:</strong> <code>labs/rest/api.js</code> (~230 lines, no framework).<br><strong>Time:</strong> 40 minutes reading, 60 minutes in the lab and the code.</div>\n<h2 id=\"the-idea\">The idea</h2>\n<p>REST models a system as <strong>resources with addresses</strong>, manipulated with a small fixed set of\nverbs. Its bet: HTTP already knows how to fetch, cache, retry, authorise and version\naddressable things, so reuse that instead of inventing your own.</p>\n<p>Roy Fielding's constraints, in the form that matters day to day:</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Constraint</th><th>What it means for you</th></tr></thead><tbody><tr><td>Client–server</td><td>separate concerns; each evolves alone</td></tr><tr><td><strong>Stateless</strong></td><td>each request carries everything needed; any server can answer</td></tr><tr><td>Cacheable</td><td>responses say whether and how long they may be reused</td></tr><tr><td>Uniform interface</td><td>same verbs, same status codes, everywhere</td></tr><tr><td>Layered system</td><td>proxies, CDNs and gateways can sit in the middle unmodified</td></tr><tr><td>Code on demand (optional)</td><td>rarely used</td></tr></tbody></table></div>\n<p>Most \"REST APIs\" are really HTTP JSON APIs, and that is fine. The valuable part is the uniform\ninterface plus statelessness plus caching — not hypermedia purity.</p>\n<h2 id=\"resource-modelling\">Resource modelling</h2>\n<pre class=\"code\"><code>/artists                      collection\n/artists/{id}                 one resource\n/artists/{id}/albums          sub-collection (relationship)\n/albums/{id}                  the album is also addressable on its own\n/albums/{id}/tracks           sub-collection\n/tracks/{id}</code></pre>\n<p>Rules that hold up:</p>\n<ul><li><strong>Nouns, plural, lowercase.</strong> <code>/artists</code>, never <code>/getArtist</code> or <code>/Artist</code>.</li><li><strong>Nest one level, then link.</strong> <code>/artists/a1/albums</code> is fine; <code>/artists/a1/albums/b1/tracks/t1</code> is a maintenance trap — <code>/tracks/t1</code> is enough.</li><li><strong>Actions that are not CRUD</strong> get modelled as resources where possible (<code>POST /albums/b1/publications</code>) or as a clearly-named sub-resource (<code>POST /orders/o1/cancellation</code>). A verb endpoint is a last resort, not a first instinct.</li><li><strong>Query strings modify a collection view</strong>, they do not identify resources: <code>?limit</code>, <code>?cursor</code>, <code>?country=IE</code>, <code>?fields=id,name</code>, <code>?sort=-year</code>.</li></ul>\n<h2 id=\"methods-and-status-codes-as-used-in-the-lab\">Methods and status codes, as used in the lab</h2>\n<div class=\"tablewrap\"><table><thead><tr><th>Call</th><th>Status</th><th>Notes</th></tr></thead><tbody><tr><td><code>GET /v1/artists?limit=2</code></td><td><code>200</code> + <code>ETag</code></td><td>paginated collection</td></tr><tr><td><code>GET /v1/artists/a1</code></td><td><code>200</code></td><td>one resource</td></tr><tr><td><code>GET</code> again with <code>If-None-Match</code></td><td><strong><code>304</code></strong>, empty body</td><td>the cheapest response you can send</td></tr><tr><td><code>GET /v1/artists/nope</code></td><td><code>404</code> + <code>application/problem+json</code></td><td>absent, permanently</td></tr><tr><td><code>POST /v1/artists</code> (valid)</td><td><strong><code>201</code></strong> + <code>Location:</code></td><td>tells the client where the new thing lives</td></tr><tr><td><code>POST /v1/artists</code> (invalid)</td><td><code>422</code> + field-level errors</td><td>well-formed, but wrong</td></tr><tr><td><code>POST</code> again with the same <code>Idempotency-Key</code></td><td><code>200</code> + <code>Idempotent-Replay: true</code></td><td>no duplicate created</td></tr><tr><td><code>PATCH /v1/artists/a2</code></td><td><code>200</code></td><td>partial update</td></tr><tr><td><code>DELETE /v1/artists/a2</code></td><td><code>204</code>, no body</td><td>nothing left to say</td></tr><tr><td>Wrong method on a collection</td><td><code>405</code> + <code>Allow: GET, POST</code></td><td>tells you what <em>is</em> allowed</td></tr></tbody></table></div>\n<p>Press each of these in the lab and read the <em>Response headers</em> panel. The status line and the\nheaders carry as much of the contract as the body does.</p>\n<p><code>PUT</code> vs <code>PATCH</code>: <code>PUT</code> replaces the whole representation (idempotent, and it deletes fields\nyou omit); <code>PATCH</code> applies a partial change. Most APIs want <code>PATCH</code> and accidentally document\n<code>PUT</code>.</p>\n<h2 id=\"caching-rest-s-biggest-structural-advantage\">Caching — REST's biggest structural advantage</h2>\n<pre class=\"code lang-js\"><code>// labs/rest/api.js\nfunction maybe304(req, res, body, extraHeaders = {}) {\n  const etag = etagOf(body);\n  if (req.headers['if-none-match'] === etag) {\n    res.writeHead(304, { ETag: etag });    // no body at all\n    res.end();\n    return 0;\n  }\n  return send(res, 200, body, { ETag: etag, 'Cache-Control': 'public, max-age=60' });\n}</code></pre>\n<p>Eleven lines gives you: browser caching, CDN caching, conditional revalidation, and a <code>304</code>\nthat costs zero payload bytes. This works <strong>because a URL identifies one stable thing</strong> — the\nproperty GraphQL and gRPC give up.</p>\n<p>Cache-control vocabulary worth knowing: <code>public</code> vs <code>private</code>, <code>max-age</code>, <code>no-store</code> (never\npersist — use for anything sensitive), <code>must-revalidate</code>, and <code>Vary</code> (which request headers\nchange the response; get this wrong and users see each other's data).</p>\n<h2 id=\"pagination\">Pagination</h2>\n<p>The lab uses opaque cursors:</p>\n<pre class=\"code lang-jsonc\"><code>{\n  \"data\": [ /* … */ ],\n  \"page\": { \"limit\": 2, \"next\": \"/rest/v1/artists?limit=2&amp;cursor=YTI\" }\n}</code></pre>\n<ul><li>Opaque so clients cannot compute it, which keeps your implementation free.</li><li>The <code>next</code> link is <em>ready to use</em> — the client concatenates nothing.</li><li><code>limit</code> is capped server-side (50 here) no matter what is requested.</li><li>Offset pagination (<code>?page=3</code>) is fine for small, stable datasets and terrible for large or changing ones: rows shift under the reader and deep offsets scan the whole table.</li></ul>\n<h2 id=\"over-fetching-under-fetching-and-the-n-1-waterfall\">Over-fetching, under-fetching and the N+1 waterfall</h2>\n<p>Small resources cache beautifully and cost round trips. Rendering one artist page in the lab:</p>\n<pre class=\"code\"><code>GET /artists/a1            →  1 trip\nGET /artists/a1/albums     →  1 trip\nGET /albums/b1/tracks      ┐\nGET /albums/b2/tracks      ┘  N trips, one per album</code></pre>\n<p>Four trips here; twenty for a busy artist. Mitigations, in the order you should reach for them:</p>\n<ol><li><strong>Sparse fieldsets</strong> — <code>?fields=id,name</code> to send less. (Fixes over-fetching, not trips.)</li><li><strong>Compound documents</strong> — <code>?expand=tracks</code> to send more per trip. Fewer trips, bigger and less cacheable payloads. Use narrowly; an <code>?expand</code> that takes ten values is a query language wearing a REST costume.</li><li><strong>Purpose-built endpoints</strong> — <code>/artist-pages/a1</code> for one important screen. Pragmatic and honest; the risk is one endpoint per screen forever.</li><li><strong>A different style</strong> — this is precisely where GraphQL earns its keep (Lesson 7).</li></ol>\n<p>Run tab 6's benchmark and look at <em>round trips</em> and <em>bytes</em> together. <code>?expand</code> cut nothing\nhere because the page still needed the album list first — a <strong>dependent</strong> call chain that no\namount of multiplexing removes.</p>\n<h2 id=\"versioning\">Versioning</h2>\n<ul><li><strong>URL versioning</strong> (<code>/v1/…</code>) — ugly, obvious, and by far the easiest to operate. Used here.</li><li><strong>Header versioning</strong> (<code>Accept: application/vnd.riff.v2+json</code>) — purer, harder to debug, easy for a client to get wrong invisibly.</li><li><strong>No versioning, additive only</strong> — works when you control all clients.</li></ul>\n<p>Whatever you pick: additive changes never bump the version, breaking changes always do, both\nversions run side by side, and deprecation comes with a date and per-consumer usage metrics.</p>\n<h2 id=\"when-rest-is-the-right-answer\">When REST is the right answer</h2>\n<ul><li>Public APIs, partner APIs, anything a stranger must integrate with using tools they have.</li><li>Resource-shaped domains: documents, entities, CRUD with real business rules.</li><li>Read-heavy workloads where HTTP caching does the heavy lifting.</li><li>Any team that needs <code>curl</code> and a browser to be sufficient debugging tools.</li></ul>\n<h2 id=\"when-it-hurts\">When it hurts</h2>\n<ul><li>Deeply nested, client-specific view data (the N+1 waterfall).</li><li>Many client types wanting very different slices of the same graph.</li><li>Ultra-high-volume internal RPC where JSON parsing shows up in profiles.</li><li>Streaming or bidirectional communication — possible with SSE/WebSocket, never native.</li></ul>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Model a domain as resources and defend where you stopped nesting.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose the right status code without looking it up, including <code>201</code>, <code>204</code>, <code>304</code>, <code>422</code>, <code>429</code>.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Implement conditional GET and explain what it saves.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain cursor vs offset pagination to a colleague who wants <code>?page=</code>.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Diagnose an N+1 waterfall and name three fixes before reaching for GraphQL.</li></ul>\n<p><strong>Exercises:</strong> <span class=\"offbook\"><code>exercises/exercises.md</code></span> §REST — add a <code>playlists</code>\nresource with pagination, ETags, <code>201 + Location</code>, and idempotent creation.</p>\n<p>Next: <a href=\"#/api-design/7\">Lesson 7 — GraphQL in depth</a></p>","words":1178,"minutes":6,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Model a domain as resources and defend where you stopped nesting.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose the right status code without looking it up, including <code>201</code>, <code>204</code>, <code>304</code>, <code>422</code>, <code>429</code>.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Implement conditional GET and explain what it saves.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain cursor vs offset pagination to a colleague who wants <code>?page=</code>.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Diagnose an N+1 waterfall and name three fixes before reaching for GraphQL.</li></ul>"},{"n":7,"title":"GraphQL in depth","file":"07-graphql.md","slug":"graphql-in-depth","html":"<div class=\"callout\"><strong>Lab:</strong> tab <strong>4 · GraphQL</strong>. <strong>Source:</strong> <code>labs/graphql/schema.js</code>, <code>labs/graphql/api.js</code>.<br><strong>Time:</strong> 40 minutes reading, 60 minutes in the lab.</div>\n<h2 id=\"the-idea\">The idea</h2>\n<p>One endpoint, one strongly-typed <strong>graph</strong> of your domain, and the <strong>client</strong> declares the\nshape of the response. Where REST's server decides what a resource looks like, GraphQL's\nserver publishes what is <em>available</em> and each query picks a subtree.</p>\n<pre class=\"code lang-graphql\"><code>{                              →   {\n  artist(id: \"a1\") {                 \"data\": {\n    name                               \"artist\": {\n    albums {                             \"name\": \"Nova Kim\",\n      title                              \"albums\": [\n      tracks { title }                     { \"title\": \"Static Garden\",\n    }                                        \"tracks\": [ { \"title\": \"Tin Roof\" } ] }\n  }                                        ]\n}                                        }\n                                       }\n                                     }</code></pre>\n<p>The response mirrors the query exactly. That is the whole pitch: no over-fetching, no\nunder-fetching, no waterfall — one round trip for a screen's worth of data.</p>\n<h2 id=\"the-schema-is-the-contract\">The schema is the contract</h2>\n<pre class=\"code lang-graphql\"><code>type Artist {\n  id: ID!\n  name: String!\n  albums(sort: AlbumSort = YEAR_DESC): [Album!]!\n}\n\ntype Album {\n  id: ID!\n  title: String!\n  artist: Artist!\n  tracks: [Track!]!\n  runtimeSeconds: Int!      # derived on the server, so every client agrees\n}\n\ntype Query    { artist(id: ID!): Artist, artists(country: String): [Artist!]! }\ntype Mutation { addArtist(input: NewArtist!): Artist! }</code></pre>\n<ul><li><code>!</code> means non-null; <code>[Album!]!</code> is a non-null list of non-null albums.</li><li><strong>Introspection</strong>: clients can ask the server for the schema at runtime — which is why GraphQL tooling (autocomplete, explorers, codegen) is so good out of the box. Press <em>View schema (SDL)</em> in the lab.</li><li>There is <strong>no second document to keep in sync</strong>. The schema is the docs, the validation and the type source for generated clients.</li></ul>\n<h2 id=\"resolvers-and-where-the-cost-hides\">Resolvers and where the cost hides</h2>\n<p>Each field is resolved by a function, and a field is only resolved if it was asked for. That\nlaziness is what kills over-fetching — and it is also where the danger lives:</p>\n<pre class=\"code lang-js\"><code>class ArtistModel {\n  albums({ sort }) { return this.ctx.albumsOf(this.a.id).map(b =&gt; new AlbumModel(b, this.ctx)); }\n}\nclass AlbumModel {\n  tracks() { return this.ctx.tracksOf(this.b.id).map(t =&gt; new TrackModel(t, this.ctx)); }\n}</code></pre>\n<p>Ask for 50 artists with their albums and tracks and you have just issued 1 + 50 + N database\nqueries. <strong>GraphQL removes the round trips between client and server; it does not remove the\nN+1 inside your server.</strong> It moves the problem where your users cannot see it.</p>\n<p>The standard fix is batching + per-request caching (DataLoader). The lab has the idea in eight\nlines:</p>\n<pre class=\"code lang-js\"><code>export function makeContext({ batch = false } = {}) {\n  const cache = new Map();\n  const memo = (key, fn) =&gt; {\n    if (!batch) return fn();\n    if (!cache.has(key)) cache.set(key, fn());\n    return cache.get(key);\n  };\n  return { albumsOf: id =&gt; memo('albums:' + id, () =&gt; albumsOfArtist(id)),\n           tracksOf: id =&gt; memo('tracks:' + id, () =&gt; tracksOfAlbum(id)) };\n}</code></pre>\n<p><strong>Do this in the lab:</strong> run <em>two resources, one round trip</em> with batching off, note the\n<em>datastore reads</em> metric, then tick <strong>per-request batching</strong> and run it again. Same response,\nfewer reads. That gap is the difference between a GraphQL API that scales and one that melts\nthe database on a Tuesday.</p>\n<h2 id=\"queries-mutations-subscriptions\">Queries, mutations, subscriptions</h2>\n<ul><li><strong>Query</strong> — reads. Fields execute in parallel.</li><li><strong>Mutation</strong> — writes. Top-level fields execute <strong>in series</strong>, so a mutation with three fields is three sequential operations. That serial guarantee is the only real difference.</li><li><strong>Subscription</strong> — a long-lived stream, usually over WebSocket. Powerful, and a separate operational commitment (connection state, scaling, auth on reconnect).</li></ul>\n<p>Also worth knowing: <strong>variables</strong> (<code>query($id: ID!)</code>) so queries are static and cacheable,\n<strong>aliases</strong> (ask for the same field twice with different arguments — try the *arguments +\naliases* preset), and <strong>fragments</strong> (reusable selection sets that mirror UI components).</p>\n<h2 id=\"what-you-give-up\">What you give up</h2>\n<h3 id=\"1-http-caching\">1. HTTP caching</h3>\n<p>Every request is <code>POST /graphql</code>. No URLs, no <code>ETag</code>, no CDN, no <code>304</code>. You replace it with:</p>\n<ul><li><strong>persisted queries</strong> — clients send a hash; the server maps it to a known query, which can then be <code>GET</code>-able and CDN-cacheable</li><li><strong>client-side normalised caches</strong> (Apollo, Relay, urql) keyed by type + id</li><li><strong><code>@cacheControl</code>-style hints</strong> aggregated per response</li></ul>\n<p>That is real machinery to build and operate, replacing something REST got from the protocol.</p>\n<h3 id=\"2-status-codes\">2. Status codes</h3>\n<p>GraphQL answers <code>200</code> almost always. Failures arrive as an <code>errors</code> array, often <em>alongside</em>\npartial <code>data</code> — a genuine feature (one broken field does not fail the screen) with a real\ncost: dashboards, alerts and retry middleware that watch status codes see nothing wrong. Run\nthe <em>a field that does not exist</em> preset and look at the HTTP status in the metrics row.</p>\n<h3 id=\"3-predictable-cost\">3. Predictable cost</h3>\n<p>A client can write a legal query that is catastrophically expensive. The mitigations are not\noptional in production:</p>\n<ul><li><strong>depth limiting</strong> — the lab rejects anything deeper than 8; run the <em>malicious deep query</em> preset and watch it get refused</li><li><strong>cost/complexity analysis</strong> — assign weights per field, reject over a budget</li><li><strong>persisted-query allow-lists</strong> — in a first-party app, only queries you shipped may run</li><li><strong>timeouts and pagination</strong> on every list field</li><li><strong>disable introspection in production</strong> for non-public APIs</li></ul>\n<h3 id=\"4-observability\">4. Observability</h3>\n<p>\"One endpoint, one status code\" defeats per-endpoint metrics. You need per-<strong>operation</strong> and\nper-<strong>field</strong> instrumentation (<code>operationName</code>, resolver tracing) before you go live, not after\nyour first incident.</p>\n<h2 id=\"when-graphql-is-the-right-answer\">When GraphQL is the right answer</h2>\n<ul><li>Many client types (web, iOS, Android, TV) needing different slices of one graph.</li><li>Deeply nested, view-shaped data where REST would waterfall.</li><li>Fast-moving front ends: adding a field is a client-side change, not a backend release.</li><li>A <strong>BFF / aggregation layer</strong> stitching several internal services into one client-facing graph.</li></ul>\n<h2 id=\"when-it-hurts\">When it hurts</h2>\n<ul><li>Simple CRUD with one client — you have bought a lot of machinery for nothing.</li><li>Cache-heavy public reads, where HTTP caching alone would have carried you.</li><li>File uploads/downloads and binary data (possible, awkward).</li><li>Untrusted public clients, unless you commit to allow-lists and cost limits.</li><li>Small teams without capacity to run the caching, cost-limiting and tracing that GraphQL assumes you will build.</li></ul>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Write a schema with types, arguments, enums, input types and nullability that means something.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why GraphQL kills round trips but not N+1, and show the fix.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what replaces HTTP caching and status codes, and what that costs.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name four defences against a hostile or careless query.</li></ul>\n<p><strong>Exercises:</strong> <span class=\"offbook\"><code>exercises/exercises.md</code></span> §GraphQL — add <code>Playlist</code>\nto the schema, with a mutation, an argument, and a query that would N+1 without batching.</p>\n<p>Next: <a href=\"#/api-design/8\">Lesson 8 — gRPC in depth</a></p>","words":1094,"minutes":5,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Write a schema with types, arguments, enums, input types and nullability that means something.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why GraphQL kills round trips but not N+1, and show the fix.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what replaces HTTP caching and status codes, and what that costs.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name four defences against a hostile or careless query.</li></ul>"},{"n":8,"title":"gRPC in depth","file":"08-grpc.md","slug":"grpc-in-depth","html":"<div class=\"callout\"><strong>Lab:</strong> tab <strong>5 · gRPC</strong>. <strong>Source:</strong> <code>labs/grpc/catalog.proto</code>, <code>server.js</code>, <code>bridge.js</code>.<br><strong>Time:</strong> 40 minutes reading, 60 minutes in the lab.</div>\n<h2 id=\"the-idea\">The idea</h2>\n<p>Not resources, not a query language — <strong>typed function calls across the network</strong>. You define\nservices and messages in a <code>.proto</code> file, a compiler generates client and server code in every\nlanguage you use, and calls travel as binary protobuf over HTTP/2.</p>\n<pre class=\"code\"><code>        catalog.proto                       ← the single source of truth\n              │  protoc / proto-loader\n      ┌───────┴────────┐\n      ▼                ▼\n server stubs     client stubs      ← generated; a field mismatch is a build error\n (implement)      (just call it)</code></pre>\n<pre class=\"code lang-proto\"><code>service CatalogService {\n  rpc GetArtist  (GetArtistRequest)  returns (Artist);\n  rpc ListAlbums (ListAlbumsRequest) returns (AlbumList);\n  rpc WatchPlays (WatchPlaysRequest) returns (stream PlayEvent);\n}</code></pre>\n<p>The client writes <code>client.GetArtist({id: \"a1\"})</code> and gets a typed <code>Artist</code> back. No URL, no\nstatus code, no JSON parsing — the network is (almost) invisible, which is both the appeal and\nthe trap.</p>\n<h2 id=\"protobuf-field-numbers-are-the-contract\">Protobuf: field numbers are the contract</h2>\n<pre class=\"code lang-proto\"><code>message Artist {\n  string id      = 1;\n  string name    = 2;\n  string country = 3;\n}</code></pre>\n<p>On the wire, protobuf writes <strong>tag numbers</strong>, not names. Consequences you must internalise:</p>\n<ul><li><strong>Renaming a field is free</strong> — the wire format never knew the name.</li><li><strong>Reusing a retired tag number silently corrupts data</strong> — an old client's <code>country</code> lands in your new <code>formed</code>. Mark retired tags <code>reserved 3;</code>.</li><li><strong>Adding a field is safe</strong> — unknown fields are ignored by old readers and, in proto3, preserved on round trips.</li><li><strong>Changing a type is not safe.</strong> <code>int32</code> → <code>string</code> on the same tag is a data corruption bug that no test with matching client and server will ever catch.</li><li><strong>Everything is optional in proto3</strong>, with typed zero values (<code>\"\"</code>, <code>0</code>, <code>false</code>). \"Absent\" and \"empty\" look identical unless you use <code>optional</code> or a wrapper — decide deliberately.</li></ul>\n<p>The payoff is measurable. In the lab, <code>ListAlbums</code> with tracks is <strong>199 protobuf bytes vs 523\nbytes of the equivalent JSON — 62 % smaller</strong> — plus a parse that is a memory copy rather than\na string scan.</p>\n<h2 id=\"the-four-call-types\">The four call types</h2>\n<div class=\"tablewrap\"><table><thead><tr><th>Type</th><th>Signature</th><th>Use</th></tr></thead><tbody><tr><td><strong>Unary</strong></td><td><code>rpc Get (Req) returns (Res)</code></td><td>the 95 % case</td></tr><tr><td><strong>Server streaming</strong></td><td><code>returns (stream Res)</code></td><td>live feeds, large result sets, progress</td></tr><tr><td><strong>Client streaming</strong></td><td><code>(stream Req) returns (Res)</code></td><td>uploads, batched telemetry</td></tr><tr><td><strong>Bidirectional</strong></td><td><code>(stream Req) returns (stream Res)</code></td><td>chat, sync, multiplexed sessions</td></tr></tbody></table></div>\n<p>All four ride one HTTP/2 connection with no bolt-on protocol. Press <strong>WatchPlays</strong> in the lab:\none request, ten responses arriving over several seconds, one connection. Expressing that in\nREST means SSE, WebSocket or polling — all of which you would have to design yourself.</p>\n<h2 id=\"status-codes-deadlines-and-the-things-rest-gave-you-for-free\">Status codes, deadlines, and the things REST gave you for free</h2>\n<p>gRPC has its <strong>own</strong> status vocabulary — <code>OK</code>, <code>INVALID_ARGUMENT</code>, <code>NOT_FOUND</code>,\n<code>PERMISSION_DENIED</code>, <code>RESOURCE_EXHAUSTED</code>, <code>FAILED_PRECONDITION</code>, <code>UNAVAILABLE</code>,\n<code>DEADLINE_EXCEEDED</code> — and they are <strong>not</strong> HTTP status codes. When you put a gateway in front,\nmapping between the two vocabularies is real design work: <code>NOT_FOUND (5)</code> → <code>404</code>,\n<code>INVALID_ARGUMENT (3)</code> → <code>400</code>, <code>FAILED_PRECONDITION (9)</code> → <code>409</code> or <code>422</code>, and so on. Do it in\none place, deliberately, or every service will invent its own mapping.</p>\n<p>Three habits gRPC makes easy and you should adopt everywhere:</p>\n<pre class=\"code lang-js\"><code>const deadline = new Date(Date.now() + 2000);         // labs/grpc/bridge.js\nclient.GetArtist({ id }, { deadline }, callback);</code></pre>\n<ul><li><strong>Deadlines on every call</strong> — propagated across hops, so a slow leaf cancels the whole chain instead of piling up work nobody is waiting for.</li><li><strong>Cancellation</strong> — the client hanging up actually stops the server (the lab's stream clears its interval on <code>cancelled</code>).</li><li><strong>Metadata</strong> — key/value pairs alongside the message, for auth tokens and tracing headers.</li></ul>\n<p>Retries need care: unary calls are only safely retryable if the method is idempotent, exactly as\nin REST. The protocol is different; the distributed-systems physics is not.</p>\n<h2 id=\"why-your-browser-cannot-call-it\">Why your browser cannot call it</h2>\n<p>Browsers give JavaScript no control over HTTP/2 frames or trailers, so a page cannot speak gRPC.\nOptions:</p>\n<ul><li><strong>gRPC-Web</strong> + a proxy (Envoy, or a framework equivalent) that translates.</li><li><strong>A gateway</strong> exposing REST/JSON or GraphQL to the outside and gRPC inside — the common shape.</li><li><strong>Connect / gRPC-compatible protocols</strong> that work over HTTP/1.1 as well.</li></ul>\n<p>The lab uses a hand-rolled version of exactly this: <code>labs/grpc/bridge.js</code> is a gRPC <em>client</em>\nthat re-emits results as JSON and SSE for the page. That extra hop is the honest cost of gRPC\nat the edge, and it is the main reason the standard architecture is <strong>gRPC internally,\nREST or GraphQL publicly</strong>.</p>\n<h2 id=\"designing-good-grpc-services\">Designing good gRPC services</h2>\n<ul><li><strong>Name RPCs as verbs on resources</strong>: <code>GetArtist</code>, <code>ListAlbums</code>, <code>CreatePlaylist</code>, <code>BatchUpdateTracks</code>. Follow Google's AIP conventions unless you have a reason not to.</li><li><strong>Wrap every request and response in its own message type</strong> — <code>GetArtistRequest</code>, not <code>string</code>. Adding a field later then never changes the signature.</li><li><strong>Version in the package</strong>: <code>package riff.catalog.v1;</code> → a v2 can live beside it.</li><li><strong>Paginate</strong> with <code>page_size</code> / <code>page_token</code>; the network being fast does not make unbounded lists safe.</li><li><strong>Reserve</strong> removed tags and names: <code>reserved 4; reserved \"old_name\";</code></li><li><strong>Errors carry detail</strong>: use <code>google.rpc.Status</code> details rather than stuffing prose into the message string.</li><li><strong>Do not leak internal types</strong> into a service other teams depend on. A <code>.proto</code> is a public contract exactly like a URL is.</li></ul>\n<h2 id=\"when-grpc-is-the-right-answer\">When gRPC is the right answer</h2>\n<ul><li>Service-to-service inside your own perimeter, where both sides deploy on your schedule.</li><li>High call volume where serialisation cost and payload size show up in profiles.</li><li>Polyglot systems: one <code>.proto</code>, real clients in Go, Java, Python, Node, Rust.</li><li>Streaming as a first-class need.</li><li>Teams that want the contract enforced by a compiler, not a code review.</li></ul>\n<h2 id=\"when-it-hurts\">When it hurts</h2>\n<ul><li>Browser-facing APIs (needs a proxy) and public APIs (needs your consumers to adopt tooling).</li><li>Human debugging: you cannot <code>curl</code> it and read the answer; you need <code>grpcurl</code> and the proto.</li><li>Caching: no HTTP caching, no CDN. You build any caching you want.</li><li>Build complexity: codegen in CI, proto registry, version discipline across repos.</li><li>Small systems where the strictness costs more than it saves.</li></ul>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Write a <code>.proto</code> with messages, a service, and a streaming RPC.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why field numbers matter more than field names, and what <code>reserved</code> prevents.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose between the four call types for a given problem.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain deadlines and cancellation, and why they belong in every RPC system.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say precisely why a browser needs a gateway, and where you would put it.</li></ul>\n<p><strong>Exercises:</strong> <span class=\"offbook\"><code>exercises/exercises.md</code></span> §gRPC — add <code>Playlist</code>\nmessages, a <code>CreatePlaylist</code> RPC with <code>INVALID_ARGUMENT</code> handling, and a streaming RPC; then\nmeasure the protobuf-vs-JSON size difference for your new message.</p>\n<p>Next: <a href=\"#/api-design/9\">Lesson 9 — Choosing and evolving</a></p>","words":1157,"minutes":6,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Write a <code>.proto</code> with messages, a service, and a streaming RPC.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why field numbers matter more than field names, and what <code>reserved</code> prevents.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose between the four call types for a given problem.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain deadlines and cancellation, and why they belong in every RPC system.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say precisely why a browser needs a gateway, and where you would put it.</li></ul>"},{"n":9,"title":"Choosing a style, and living with it afterwards","file":"09-choosing-and-evolving.md","slug":"choosing-a-style-and-living-with-it-afterwards","html":"<div class=\"callout\"><strong>Lab:</strong> tab <strong>6 · Compare</strong> — run the benchmark before you read the table below.<br><strong>Worksheet:</strong> <span class=\"offbook\"><code>worksheets/protocol-decision-worksheet.md</code></span><br><strong>Time:</strong> 30 minutes reading, 60 minutes producing your decision record.</div>\n<h2 id=\"the-measurement-first\">The measurement first</h2>\n<p>\"Render one artist page: the artist, their albums, every track.\" Same data, same machine:</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Style</th><th>Round trips</th><th>Bytes</th><th>What the number hides</th></tr></thead><tbody><tr><td>REST (naive)</td><td>4</td><td>2,568</td><td>on 4G that is ~400 ms of pure latency</td></tr><tr><td>REST (<code>?expand=tracks</code>)</td><td>4</td><td>3,106</td><td>bigger payloads, fewer cacheable pieces</td></tr><tr><td>GraphQL</td><td>1</td><td>795</td><td>one round trip; server-side reads unchanged unless you batch</td></tr><tr><td>gRPC</td><td>1</td><td>199</td><td>protobuf; the browser cannot call it without a gateway</td></tr></tbody></table></div>\n<p>Run it yourself before trusting these. Then remember what localhost hides: <strong>round trips are\nnearly free here and expensive everywhere else</strong>. A benchmark that ignores your consumer's\nnetwork is a benchmark about your laptop.</p>\n<h2 id=\"the-decision-honestly\">The decision, honestly</h2>\n<pre class=\"code\"><code>Does a browser or a stranger call it directly?\n├── yes\n│    └── Do different clients need very different slices of one graph?\n│         ├── yes → GraphQL at the edge (and budget for caching + cost limits)\n│         └── no  → REST + HTTP caching\n└── no — service to service\n     └── High volume, strict types, or streaming?\n          ├── yes → gRPC\n          └── no  → REST is fine. Boring is a feature.</code></pre>\n<p>Most real systems answer <strong>all three</strong>: gRPC between services, REST or GraphQL at the edge,\nevents for anything that must not block. \"Which one\" is usually the wrong question; \"which one\n*here*\" is the right one.</p>\n<h2 id=\"the-factors-that-actually-decide-it\">The factors that actually decide it</h2>\n<div class=\"tablewrap\"><table><thead><tr><th>Factor</th><th>Pushes you to</th></tr></thead><tbody><tr><td>Consumers you do not control</td><td>REST</td></tr><tr><td>Many client shapes, one graph</td><td>GraphQL</td></tr><tr><td>Same-team services, high call volume</td><td>gRPC</td></tr><tr><td>Read-heavy, cacheable</td><td>REST</td></tr><tr><td>Streaming or bidirectional</td><td>gRPC (or SSE/WebSocket beside REST)</td></tr><tr><td>Strict typing enforced by a compiler</td><td>gRPC</td></tr><tr><td>Team has never run it before</td><td>whatever you can operate at 3 a.m.</td></tr><tr><td>Debuggability with a terminal</td><td>REST</td></tr><tr><td>Payload size at scale</td><td>gRPC</td></tr><tr><td>Front end iterating faster than the backend</td><td>GraphQL</td></tr></tbody></table></div>\n<p>That second-to-last row is not a joke. Operability beats elegance: an API style your team can\ndebug, monitor and explain to a new hire will outperform a better-suited one they cannot.</p>\n<h2 id=\"costs-people-forget-to-count\">Costs people forget to count</h2>\n<ul><li><strong>GraphQL</strong>: normalised client caches, persisted queries, depth and cost limits, per-field tracing, schema governance across teams. You are adopting a platform, not a library.</li><li><strong>gRPC</strong>: codegen in CI, a proto registry or shared repo, gateway for browsers, <code>grpcurl</code> in everyone's toolkit, version discipline across languages.</li><li><strong>REST</strong>: the discipline to stay consistent across dozens of endpoints and years, plus bespoke solutions when a screen really does need a graph.</li></ul>\n<h2 id=\"living-with-the-choice-evolution\">Living with the choice: evolution</h2>\n<p>Once someone integrates, your contract is a liability you maintain. The rules are the same in\nall three styles:</p>\n<p><strong>Always safe (additive):</strong> new endpoint / type / RPC; new optional field; new enum value <em>if</em>\nclients were told to tolerate unknowns; relaxed validation.</p>\n<p><strong>Always breaking:</strong> removing or renaming; changing a type; making an optional field required;\ntightening validation; changing a default; changing the meaning of an existing field.</p>\n<div class=\"tablewrap\"><table><thead><tr><th></th><th>REST</th><th>GraphQL</th><th>gRPC</th></tr></thead><tbody><tr><td>Add a field</td><td>safe</td><td>safe (nobody selects it unasked)</td><td>safe (new tag)</td></tr><tr><td>Remove a field</td><td>breaking → <code>/v2</code></td><td><code>@deprecated</code> + measure field usage, then remove</td><td><code>reserved</code> the tag, never reuse</td></tr><tr><td>Version</td><td><code>/v1</code>, <code>/v2</code> side by side</td><td>one evolving schema; versioning is a smell</td><td><code>package …v1</code> → <code>…v2</code></td></tr><tr><td>Deprecation signal</td><td><code>Deprecation</code> / <code>Sunset</code> headers, changelog</td><td><code>@deprecated(reason:)</code> in the schema</td><td>comment + registry policy</td></tr></tbody></table></div>\n<p>GraphQL's field-level usage stats make it the one style where you can <em>prove</em> nobody uses a\nfield before deleting it. Use that; it is the strongest argument for the schema-first world.</p>\n<h2 id=\"the-deprecation-playbook\">The deprecation playbook</h2>\n<ol><li><strong>Announce</strong> with a date, in the changelog and in the response (<code>Deprecation</code>, <code>Sunset</code>, or <code>@deprecated</code>).</li><li><strong>Measure</strong> usage per consumer. If you cannot, you cannot deprecate — instrument first.</li><li><strong>Contact</strong> the top consumers directly. A dashboard is not a conversation.</li><li><strong>Brown-out</strong>: fail the endpoint for short windows near the date so quiet clients notice while it is still recoverable.</li><li><strong>Remove</strong>, and keep a clear error explaining what replaced it.</li></ol>\n<h2 id=\"common-failure-modes-named\">Common failure modes, named</h2>\n<ul><li><strong>The database viewer</strong> — endpoints mirroring tables; every schema change breaks a client.</li><li><strong>The chatty mobile app</strong> — REST N+1 over a cell network; 2 s screen loads that look fine on the office wifi.</li><li><strong>The GraphQL DB melt</strong> — one legal query, no depth or cost limits, one bad afternoon.</li><li><strong>The distributed monolith</strong> — gRPC everywhere, every service deploying in lockstep; you have bought network latency and kept all the coupling.</li><li><strong>The v1 that never dies</strong> — no usage metrics, so no deprecation is ever safe.</li><li><strong>The unversioned public API</strong> — silently breaking changes, and integrators who never trust you again.</li></ul>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose a style for a concrete case and defend it with your own measurements.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;List what each style costs to <em>operate</em>, not just to write.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Classify any proposed change as additive or breaking without hesitating.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Run a deprecation that does not surprise anyone.</li></ul>\n<p><strong>Artifact for this module (and the course):</strong> a one-page decision record for a real project —\ncontext, the jobs, the styles considered, the measured numbers, the choice, the operational\ncosts you are accepting, and what evidence would make you change your mind. That document is\nthe deliverable a staff engineer would ask for, and producing it is the proof you learned this.</p>\n<p>Back to the <a href=\"#/api-design\">course plan</a>.</p>","words":1011,"minutes":5,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose a style for a concrete case and defend it with your own measurements.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;List what each style costs to <em>operate</em>, not just to write.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Classify any proposed change as additive or breaking without hesitating.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Run a deprecation that does not surprise anyone.</li></ul>"}]},{"id":"data-storage","dir":"Data & Storage","title":"Data & Storage","numeral":"II","blurb":"What a read actually costs. Query plans, indexes, transactions and locks, against a real Postgres holding a million rows.","lab":"http://localhost:4100","lessons":[{"n":1,"title":"Why storage is the hard part","file":"01-why-storage-is-the-hard-part.md","slug":"why-storage-is-the-hard-part","html":"<p>In the API Design course you built three contracts over the same music catalogue and measured\nwhat each one cost. The compare benchmark counted round trips and payload bytes, and one column\nin that table was quietly lying to you: the datastore.</p>\n<p>Every one of those API calls ended in a read. The lab served them from a JavaScript array, so\neach read cost nothing and the benchmark treated storage as free. It is not free. In a real\nsystem it is usually the largest single term in your latency budget, and it is the term that\ndoes not get better when you add servers.</p>\n<p>This course opens that box.</p>\n<h2 id=\"why-storage-resists-the-tricks-that-work-everywhere-else\">Why storage resists the tricks that work everywhere else</h2>\n<p>Stateless things are easy to scale. If your API service is slow you run more copies of it. Any\nrequest can go to any copy, because no copy knows anything the others do not. This is why the\narchitecture diagrams in Lesson 02 of the API course had that reassuring row of identical boxes.</p>\n<p>Storage cannot do that, because storage is the part that <em>remembers</em>. The moment you run two\ncopies you have to answer questions with no comfortable answers:</p>\n<ul><li>If a write lands on copy A and a read goes to copy B a millisecond later, what does the reader see? The old value, the new one, or an error?</li><li>If A and B both accept a write to the same row at the same time, which one wins?</li><li>If the network between A and B fails, do you stop serving writes, or keep serving them and reconcile later?</li></ul>\n<p>Those questions have no universally right answers — only trade-offs with names, which the rest\nof this course gives you. But notice what has already happened: your stateless service scaled by\nadding boxes, and your database scaled by making you choose what you are willing to lose.</p>\n<p>That is the first reason storage is hard. Here is the second, and it is the one that will\nactually bite you this year.</p>\n<hr>\n<h2 id=\"the-cost-you-cannot-see-from-the-api\">The cost you cannot see from the API</h2>\n<p>An API contract tells a client what it can ask for. It says nothing about what answering costs.\nTwo endpoints with identical contracts, identical payloads and identical response times on your\nlaptop can differ by three orders of magnitude in production, because one of them reads twelve\nrows through an index and the other reads a million rows and throws away 999,988 of them.</p>\n<p>You measured that gap in this course's lab before you read this sentence, or you will in Lesson</p>\n<ol><li>Here it is, from the real database you are running:</li></ol>\n<pre class=\"code\"><code>SELECT count(*) FROM plays WHERE track_id = 't5';\n\n  without an index    20.2 ms    36,372 blocks read    998,379 rows discarded\n  with an index        1.3 ms         10 blocks read            0 rows discarded</code></pre>\n<p>Same SQL. Same answer. Same contract. One decision — whether an index exists — moved it by 16×\nin time and 3,600× in blocks touched. No API design review would ever catch this, because\nnothing about the API changed.</p>\n<p>This is the through-line of the whole course: <strong>the contract tells you what is possible; the\nstorage layer decides what it costs.</strong> You have learned to design the first. Now you learn to\nprice the second.</p>\n<hr>\n<h2 id=\"what-a-database-actually-gives-you\">What a database actually gives you</h2>\n<p>It is worth being precise about what you are buying, because \"we need a database\" is often said\nwithout anyone naming which of these they need:</p>\n<ol><li><strong>Durability.</strong> A write that returns success survives a power cut. This is harder than it sounds and is why databases fsync, keep a write-ahead log, and are slower than writing to a file you never flush.</li><li><strong>Concurrency control.</strong> Many clients read and write at once and the result still makes sense. This is Lessons 05 and 06, and it is where most of the genuinely surprising bugs live.</li><li><strong>Query flexibility.</strong> You can ask questions nobody anticipated when the schema was designed, and get an answer in a sensible time. This is what indexes and the query planner buy you.</li><li><strong>Integrity.</strong> Rules that hold no matter which application, script or intern is writing. Constraints, Lesson 02.</li></ol>\n<p>A cache gives you (3) and nothing else. A log file gives you (1) and nothing else. When someone\nproposes replacing your database with something faster, the useful question is which of the four\nthey are proposing to drop — because they are dropping at least one.</p>\n<hr>\n<h2 id=\"the-mental-model-it-is-all-blocks\">The mental model: it is all blocks</h2>\n<p>Underneath every abstraction in this course is one physical fact. Postgres does not read rows.\nIt reads <strong>8 KB blocks</strong> (pages), and a block is the smallest unit it will fetch, cache or write.\nEverything you are about to learn is a consequence:</p>\n<ul><li>A sequential scan is cheap <em>per block</em> and expensive because it reads every block.</li><li>An index is a structure that turns \"which rows match?\" into \"which blocks do I need?\" — and it is only worth having when the answer is \"far fewer than all of them.\"</li><li><code>shared_buffers</code> is a cache of blocks. A block already in memory is roughly 100× cheaper than one that has to come off disk, which is why the plan output in this lab reports <code>hit</code> and <code>read</code> separately.</li><li>A row update does not edit a row in place. It writes a <em>new version</em> of the row into a block and leaves the old one behind for later cleanup — which is how readers avoid blocking writers (Lesson 05) and why VACUUM exists (Lesson 08).</li></ul>\n<p>When a number in this course surprises you, count blocks. The answer is almost always there.</p>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why a stateless service scales by adding copies and a database does not.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name the four things a database gives you, and say which one a proposed replacement drops.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why two endpoints with identical contracts can differ 1000× in cost.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Describe, in terms of 8 KB blocks, why an index helps some queries and not others.</li></ul>\n<p><strong>Artifact for this module:</strong> a one-page <strong>storage cost map</strong> of a system you actually work on.\nList its top five read paths and its top three write paths. For each one, write down: which store\nanswers it, roughly how many rows it touches, whether it is indexed for that access pattern, and\nwhat happens to that number as the table grows 100×. You will not know every answer — mark the\ngaps as gaps. *An honest map with three question marks is worth more than a confident one with\nnone, and the question marks are your reading list for the rest of this course.*</p>\n<p>Next: <a href=\"#/data-storage/2\">02 · Schema design and the constraints that outlive your code</a></p>","words":1152,"minutes":6,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why a stateless service scales by adding copies and a database does not.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name the four things a database gives you, and say which one a proposed replacement drops.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why two endpoints with identical contracts can differ 1000× in cost.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Describe, in terms of 8 KB blocks, why an index helps some queries and not others.</li></ul>"},{"n":2,"title":"Schema design and the constraints that outlive your code","file":"02-schema-design-and-constraints.md","slug":"schema-design-and-the-constraints-that-outlive-your-code","html":"<p>Your application code is temporary. It gets rewritten in a new framework, ported to a new\nlanguage, replaced by a service, called from a script somebody wrote at 2am. The schema is what\nsurvives all of that, which is why a rule enforced in the schema is worth more than the same rule\nenforced in a validator.</p>\n<p>This lesson is about writing down what is true, in the one place where it stays true.</p>\n<h2 id=\"the-relational-model-in-one-paragraph\">The relational model in one paragraph</h2>\n<p>A table is a set of rows with the same shape. A row is a fact. A key identifies a row. A foreign\nkey says \"this fact refers to that fact.\" That is the entire model, and its power comes from what\nit <em>refuses</em> to let you express: no ordering you did not ask for, no duplicates you did not\ndeclare, no dangling references. Everything else — joins, indexes, transactions — is machinery\nfor making that model fast and safe.</p>\n<p>The catalogue in this lab is four facts:</p>\n<pre class=\"code\"><code>artists    one row per artist\nalbums     one row per album, referring to an artist\ntracks     one row per track, referring to an album\nplays      one row per time somebody played a track\nlisteners  one row per listener</code></pre>\n<p>Open <strong>Tab 1 · Schema</strong> in the lab. Everything shown there is queried live from\n<code>information_schema.columns</code> and <code>pg_constraint</code> — Postgres' own catalogue — rather than parsed\nout of the <code>.sql</code> file. That is deliberate. A schema diagram in a wiki starts drifting from\nreality the day after somebody runs a migration. The catalogue cannot drift, because it <em>is</em> the\nschema.</p>\n<hr>\n<h2 id=\"normalisation-without-the-vocabulary\">Normalisation, without the vocabulary</h2>\n<p>You will meet \"third normal form\" and a list of numbered rules. The useful version is one\nquestion:</p>\n<blockquote><p><strong>Is this fact stored in exactly one place?</strong></p></blockquote>\n<p>If an artist's country is stored on the artist row, changing it is one update. If it is also\ncopied onto every album for convenience, changing it is one update plus a migration plus the rows\nsomebody missed — and now two places disagree and neither is obviously wrong. That disagreement\nis called an <em>update anomaly</em>, and avoiding it is what normalisation is for.</p>\n<p>The counter-pressure is real: normalised data needs joins, and joins cost. <strong>Denormalise when you\nhave measured a join you cannot afford</strong>, not when you anticipate one. Denormalisation is an\noptimisation, and like every optimisation it is a debt you take on deliberately, with a note\nsaying what invariant you are now responsible for maintaining by hand.</p>\n<p>A useful tell: if you cannot say which copy is authoritative, you have not denormalised — you\nhave introduced a bug that has not fired yet.</p>\n<hr>\n<h2 id=\"constraints-are-the-part-you-cannot-get-back\">Constraints are the part you cannot get back</h2>\n<p>Look at the constraint table in Tab 1. Every line there is a rule Postgres enforces on every\nwrite, from every client, forever:</p>\n<pre class=\"code lang-sql\"><code>CHECK (year BETWEEN 1900 AND 2100)          -- no album released in 12024\nUNIQUE (album_id, position)                 -- no two tracks in slot 3\nREFERENCES artists(id) ON DELETE CASCADE    -- no album belonging to a deleted artist\nNOT NULL                                    -- this fact is required to be a fact</code></pre>\n<p>The argument against constraints is always the same: \"we validate that in the application.\"\nThree answers:</p>\n<ol><li><strong>You validate it in <em>an</em> application.</strong> The next one, the admin script, the data fix, and the analytics job that writes back all bypass your validator.</li><li><strong>Your validator races.</strong> Two requests both check \"is this position free?\", both see yes, both insert. A <code>UNIQUE</code> constraint is the only thing that can lose that race safely — it is checked at write time by the thing doing the writing. You will watch exactly this race in Lesson 05.</li><li><strong>A constraint is documentation that cannot be wrong.</strong> A new engineer reading <code>UNIQUE (album_id, position)</code> learns something true. A comment might be stale.</li></ol>\n<p>The cost is honest and worth stating: constraints make some migrations harder, and a <code>CHECK</code> you\nregret is a lock-taking <code>ALTER TABLE</code> away from being removed. That is the trade. Take it — the\nconstraint you can drop later is cheaper than the corrupt data you cannot un-corrupt.</p>\n<h3 id=\"the-one-postgres-does-not-do-for-you\">The one Postgres does not do for you</h3>\n<p>Read the constraint list in Tab 1 again and notice what is <em>missing</em>: there is no index on\n<code>albums.artist_id</code> or <code>tracks.album_id</code>.</p>\n<p><strong>Postgres creates indexes for <code>PRIMARY KEY</code> and <code>UNIQUE</code>. It never creates one for a foreign\nkey.</strong> Not on the referencing side, which is the side you query. This surprises almost everyone,\nand it is one of the most common causes of a database that was fine at 10,000 rows and is not\nfine at 10 million — because \"show me this artist's albums\" is a sequential scan, and so is every\n<code>ON DELETE CASCADE</code>.</p>\n<p>Lesson 04 has you fix it and measure what it was costing.</p>\n<hr>\n<h2 id=\"choosing-types-like-they-matter\">Choosing types like they matter</h2>\n<p>They do. A type is a constraint that also determines storage size:</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Instead of</th><th>Use</th><th>Why</th></tr></thead><tbody><tr><td><code>varchar(255)</code></td><td><code>text</code></td><td>In Postgres they are the same speed. The 255 is a folk memory from another database, and it will be wrong eventually.</td></tr><tr><td><code>float</code> for money</td><td><code>numeric</code></td><td>Binary floats cannot represent 0.10. Do not find this out from an accountant.</td></tr><tr><td><code>timestamp</code></td><td><code>timestamptz</code></td><td>Stores an instant rather than an ambiguous wall-clock reading. The one you want is almost always this one.</td></tr><tr><td><code>text</code> for enums</td><td><code>text</code> + <code>CHECK</code>, or an enum type</td><td><code>plays.device</code> in this schema is <code>CHECK (device IN (...))</code> — typos become errors instead of a fifth device nobody notices for a year.</td></tr><tr><td><code>serial</code></td><td><code>bigint</code> / <code>bigserial</code></td><td>An <code>int</code> primary key runs out at 2.1 billion. Migrating a live table's key type is a genuinely bad week.</td></tr></tbody></table></div>\n<p><code>plays.id</code> in this lab is <code>bigserial</code> for exactly that reason. A million rows today, and the\ncolumn costs four extra bytes to never think about it again.</p>\n<hr>\n<h2 id=\"design-the-schema-around-the-questions\">Design the schema around the questions</h2>\n<p>The API course told you to shape endpoints around what clients need to accomplish, not around\nyour tables. The storage version of that rule points the other way:</p>\n<blockquote><p><strong>Shape tables around the facts. Shape <em>indexes</em> around the questions.</strong></p></blockquote>\n<p>Keep the schema normalised and honest, then let Lesson 04's indexes serve the specific access\npatterns. This split matters because facts change rarely and questions change constantly. A\nschema built around today's screen has to be migrated when the screen changes; a schema built\naround the facts just needs another index.</p>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;State the one question normalisation is really asking, and when to deliberately break it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Give three reasons a constraint beats the equivalent check in application code.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why <code>UNIQUE</code> is the only safe answer to a check-then-insert race.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say which indexes Postgres creates for you and which it does not.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Justify <code>text</code>, <code>numeric</code>, <code>timestamptz</code> and <code>bigint</code> over their common alternatives.</li></ul>\n<p><strong>Artifact for this module:</strong> design the schema for something you understand well that is <em>not</em>\nthis catalogue — a booking system, an invoice ledger, a permissions model, whatever you have real\nknowledge of. Write the DDL. Then, beneath each table, write one line per constraint saying <strong>what\nwould go wrong without it</strong>. If you cannot name the failure, either the constraint is not earning\nits place or you have not understood the domain yet — both are useful things to discover on paper.</p>\n<p>Next: <a href=\"#/data-storage/3\">03 · How a query actually runs</a></p>","words":1278,"minutes":6,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;State the one question normalisation is really asking, and when to deliberately break it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Give three reasons a constraint beats the equivalent check in application code.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why <code>UNIQUE</code> is the only safe answer to a check-then-insert race.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say which indexes Postgres creates for you and which it does not.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Justify <code>text</code>, <code>numeric</code>, <code>timestamptz</code> and <code>bigint</code> over their common alternatives.</li></ul>"},{"n":3,"title":"How a query actually runs","file":"03-how-a-query-actually-runs.md","slug":"how-a-query-actually-runs","html":"<p>SQL is a <em>declarative</em> language. You write what you want; you never write how to get it. That is\nthe source of its power and of every performance surprise you will ever have with it, because\nsomething else is choosing the how — and when it chooses badly, your query is slow for reasons\ninvisible in the query text.</p>\n<p>That something is the planner. This lesson teaches you to read its mind.</p>\n<h2 id=\"the-five-stages\">The five stages</h2>\n<ol><li><strong>Parse</strong> — is this valid SQL?</li><li><strong>Analyse</strong> — do these tables and columns exist, and what types are they?</li><li><strong>Rewrite</strong> — expand views, apply rules. (The <code>artist_pages</code> view in this lab disappears here; by planning time it is just a join.)</li><li><strong>Plan</strong> — consider the possible execution strategies, estimate the cost of each, pick the cheapest.</li><li><strong>Execute</strong> — run the winner.</li></ol>\n<p>Stage 4 is the whole ballgame. For a three-table join there are already a dozen plausible plans:\nwhich table to read first, whether to use an index, which join algorithm for each pair. The\nplanner estimates a cost for each and picks the minimum.</p>\n<p><strong>It estimates from statistics, not from your data.</strong> That distinction is the source of most bad\nplans, and we will come back to it.</p>\n<hr>\n<h2 id=\"reading-a-plan\">Reading a plan</h2>\n<p>Open <strong>Tab 2 · Query plans</strong>, pick <em>\"Filter on an unindexed column\"</em>, and run it. You get\nsomething like:</p>\n<pre class=\"code\"><code>Aggregate  (planned 1, actual 1)  19.76ms  [36372 blocks]\n  -&gt; Seq Scan on plays  (planned 1064, actual 1621)  19.5ms  [36372 blocks]\n       Filter: (track_id = 't5'::text)\n       Rows removed by filter: 998379</code></pre>\n<p>Read it <strong>bottom-up and inside-out</strong>: the deepest node runs first and feeds its parent.</p>\n<ul><li><strong><code>Seq Scan on plays</code></strong> — the access method. Postgres read the whole table.</li><li><strong><code>planned 1064, actual 1621</code></strong> — what the planner guessed versus what it got. Close here.</li><li><strong><code>Rows removed by filter: 998379</code></strong> — the number that should make you wince. Postgres read a million rows and threw away 998,379 of them to return 1,621. <strong>This single line is the clearest \"you are missing an index\" signal in the whole tool.</strong></li><li><strong><code>[36372 blocks]</code></strong> — 8 KB pages touched. About 284 MB of reading to answer a question about a few hundred rows.</li></ul>\n<p>Now the same query in Lesson 04, after an index exists:</p>\n<pre class=\"code\"><code>Aggregate  (actual 1)  1.25ms  [10 blocks]\n  -&gt; Index Only Scan using idx_plays_track  (actual 1621)  [10 blocks]\n       Index Cond: (track_id = 't5'::text)</code></pre>\n<p>Ten blocks instead of 36,372. No rows removed, because the index went straight to the matching\nones. That is the shape of every index win you will ever see.</p>\n<hr>\n<h2 id=\"the-access-methods-and-when-each-is-right\">The access methods, and when each is right</h2>\n<div class=\"tablewrap\"><table><thead><tr><th>Node</th><th>What it does</th><th>When it is correct</th></tr></thead><tbody><tr><td><strong>Seq Scan</strong></td><td>Read every block.</td><td>You want a large fraction of the table. Genuinely optimal for \"count all rows\" — an index would be slower.</td></tr><tr><td><strong>Index Scan</strong></td><td>Walk the index, fetch each matching row from the heap.</td><td>Few matching rows, and you need columns the index does not hold.</td></tr><tr><td><strong>Bitmap Heap Scan</strong></td><td>Collect all matching row locations first, sort them, then read the heap in <em>page order</em>.</td><td>Medium number of matches. Turns scattered random reads into something closer to sequential.</td></tr><tr><td><strong>Index Only Scan</strong></td><td>The index contained every column the query needed. The table is never touched.</td><td>The cheapest read available — and it depends on VACUUM keeping the visibility map fresh (Lesson 08).</td></tr></tbody></table></div>\n<p><strong>A sequential scan is not a bug.</strong> If you ask for 80% of a table, reading it straight through is\nthe right plan and an index would be slower — every indexed row costs an extra hop to the heap.\nThe planner knows this. When it chooses a seq scan for a query returning three rows, the problem\nis not the planner's preference; it is that the planner does not believe it will only get three\nrows. Which brings us to the real skill.</p>\n<hr>\n<h2 id=\"join-strategies\">Join strategies</h2>\n<p>Three ways to match two sets of rows, chosen by estimated size:</p>\n<ul><li><strong>Nested Loop</strong> — for each row on the left, look up matches on the right. Excellent when the left side is tiny and the right side is indexed. Catastrophic when the planner thought the left side was 10 rows and it is 400,000, because it now does 400,000 lookups.</li><li><strong>Hash Join</strong> — build a hash table of the smaller side in memory, stream the larger side past it. The workhorse for joining two large sets.</li><li><strong>Merge Join</strong> — sort both sides, walk them together. Good when both are already sorted, e.g. arriving from index scans in the right order.</li></ul>\n<p>Run <em>\"Join artists to albums to tracks\"</em> in the lab and look at which one you got. Then read the\nnext section to understand why.</p>\n<hr>\n<h2 id=\"the-number-that-actually-predicts-slow-queries\">The number that actually predicts slow queries</h2>\n<p>Not execution time. <strong>The ratio between planned and actual rows.</strong></p>\n<p>The lab shows this as <em>worst estimate</em>. Here is why it is the number to watch: the planner picked\na nested loop because it expected 10 rows. If it gets 400,000, it does not change its mind\npartway through — the plan is already chosen. The query is now doing 400,000 index lookups\ninstead of one hash join, and it will be slow in a way that no amount of staring at the SQL\nexplains.</p>\n<p>So when a query is inexplicably slow, the first question is never \"which index should I add?\" It\nis <strong>\"did the planner know what it was getting into?\"</strong> A 1000× mis-estimate means the fix is\nusually statistics, not indexes:</p>\n<ul><li><code>ANALYZE tablename</code> — refresh statistics. Postgres does this automatically via autovacuum, but after a bulk load the stats can be badly stale. This lab's seed runs <code>ANALYZE</code> at the end for exactly that reason; without it, every plan you read here would be a lie.</li><li><code>ALTER TABLE ... ALTER COLUMN ... SET STATISTICS 500</code> — keep a finer histogram for a column with a skewed distribution.</li><li><code>CREATE STATISTICS</code> — teach the planner that two columns are <em>correlated</em>. Postgres assumes independence by default, so for <code>WHERE country = 'KR' AND city = 'Seoul'</code> it multiplies the two selectivities and estimates far too few rows.</li></ul>\n<hr>\n<h2 id=\"explain-versus-explain-analyze\">EXPLAIN versus EXPLAIN ANALYZE</h2>\n<ul><li><code>EXPLAIN</code> — plan only. Nothing runs. Estimates only.</li><li><code>EXPLAIN ANALYZE</code> — <strong>actually runs the query</strong> and reports real timings and row counts.</li></ul>\n<p>That second point matters more than it looks. <code>EXPLAIN ANALYZE DELETE FROM ...</code> deletes the rows.\nThe safe habit, which this lab does for you automatically on any non-<code>SELECT</code>:</p>\n<pre class=\"code lang-sql\"><code>BEGIN;\nEXPLAIN (ANALYZE, BUFFERS) UPDATE plays SET ms_played = 0 WHERE id = 1;\nROLLBACK;</code></pre>\n<p>Always add <code>BUFFERS</code>. Time varies with cache warmth and what else the machine is doing; block\ncounts are stable, comparable between runs, and tell you the truth about how much work happened.\n<strong>When you benchmark a change, compare blocks first and milliseconds second.</strong></p>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Read a plan tree bottom-up and say which node ran first.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain what \"rows removed by filter\" means and why a large value indicates a missing index.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name the four access methods and give a case where each is the correct choice.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Argue why a sequential scan is sometimes the right plan.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why a bad row estimate causes a bad plan, and name three ways to fix the estimate.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Plan a destructive statement without executing it.</li></ul>\n<p><strong>Artifact for this module:</strong> find a genuinely slow query — in your own system if you have one,\nor the worst one you can construct in this lab. Capture its <code>EXPLAIN (ANALYZE, BUFFERS)</code> output.\nWrite a paragraph diagnosing it that names <strong>the specific node</strong> where the time or the blocks go,\nand <strong>why</strong> the planner chose it. Then fix it and capture the new plan. The artifact is the two\nplans plus the paragraph — the diagnosis is the part that proves you learned something, not the\nspeed-up.</p>\n<p>Next: <a href=\"#/data-storage/4\">04 · Indexes</a></p>","words":1372,"minutes":7,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Read a plan tree bottom-up and say which node ran first.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain what \"rows removed by filter\" means and why a large value indicates a missing index.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name the four access methods and give a case where each is the correct choice.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Argue why a sequential scan is sometimes the right plan.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why a bad row estimate causes a bad plan, and name three ways to fix the estimate.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Plan a destructive statement without executing it.</li></ul>"},{"n":4,"title":"Indexes","file":"04-indexes.md","slug":"indexes","html":"<p>An index is a deal. You agree to pay on every write, forever, and in exchange some reads get\ndramatically cheaper. The deal is usually good. It is not always good, and nobody sends you an\ninvoice for the part you are paying — which is why databases accumulate indexes that cost real\nmoney and serve nothing.</p>\n<p>This lesson is about reading the deal before you sign it.</p>\n<h2 id=\"what-a-b-tree-actually-is\">What a B-tree actually is</h2>\n<p>Postgres' default index is a B-tree: a shallow, sorted, block-structured tree. For a million-row\ntable it is about three or four levels deep, so finding any value costs three or four block\nreads instead of 36,372.</p>\n<p>Two consequences follow from \"sorted\", and they explain nearly everything an index can and cannot\ndo:</p>\n<p><strong>It can serve ordering, not just matching.</strong> <code>ORDER BY played_at DESC LIMIT 10</code> can walk the tail\nof an index on <code>played_at</code> and stop after ten rows. No sort, no scan.</p>\n<p><strong>It can serve prefixes, not suffixes.</strong> The index is sorted by its first column, then by its\nsecond within that. Like a phone book sorted by (surname, first name): finding everyone called\nKim is instant, finding everyone whose first name is Nova means reading the whole thing. This is\nthe single most important fact about composite indexes, and Lab 3 has a pair of them that prove\nit.</p>\n<hr>\n<h2 id=\"run-the-loop\">Run the loop</h2>\n<p>Open <strong>Tab 3 · Indexes</strong>. The loop is: <em>probe → create → probe again → check the write cost.</em>\nStart with <code>plays (track_id)</code>. Real numbers from this lab:</p>\n<pre class=\"code\"><code>                     before              after\ntime                 20.2 ms             1.25 ms         16× faster\nblocks               36,372              10              3,637× fewer\naccess               Seq Scan            Index Only Scan\nindex build          —                   335 ms, 7.1 MB</code></pre>\n<p>Two things to notice beyond the speed-up.</p>\n<p><strong>It became an <em>Index Only</em> Scan.</strong> The query was <code>count(*) ... WHERE track_id = 't5'</code> — it needed\nno column other than <code>track_id</code>, and the index holds <code>track_id</code>. The table was never touched at\nall. That is the cheapest read Postgres can do.</p>\n<p><strong>The blocks improved far more than the time.</strong> 3,637× fewer blocks but only 16× faster, because\nthis table fits in the page cache — the sequential scan was reading from RAM, not disk. On a\ntable larger than memory that 16× becomes hundreds. <strong>This is why block counts are the honest\nmetric and milliseconds are the flattering one.</strong></p>\n<hr>\n<h2 id=\"column-order-in-composite-indexes\">Column order in composite indexes</h2>\n<p>The lab ships two indexes over the same two columns, deliberately:</p>\n<pre class=\"code lang-sql\"><code>CREATE INDEX idx_plays_track_time ON plays (track_id, played_at);   -- right\nCREATE INDEX idx_plays_time_track ON plays (played_at, track_id);   -- wrong</code></pre>\n<p>For the query <code>WHERE track_id = 't5' AND played_at &gt; now() - interval '30 days'</code>:</p>\n<ul><li><strong><code>(track_id, played_at)</code></strong> — seek to <code>t5</code>, then walk forward through that track's plays in time order and stop at the boundary. Both predicates served by one seek.</li><li><strong><code>(played_at, track_id)</code></strong> — the leading column is the <em>range</em>. Postgres must scan every row in the 30-day window and check <code>track_id</code> on each. The second column filters; it does not seek.</li></ul>\n<p><strong>The rule: equality columns first, range columns last.</strong> Same two columns, same disk cost, very\ndifferent query — and in a code review the two lines look identical. Create them both in the lab\nand probe the same query against each.</p>\n<p>One corollary worth internalising: <code>(track_id, played_at)</code> also serves any query that filters on\n<code>track_id</code> alone. A composite index covers its own prefixes, so <code>idx_plays_track</code> becomes\nredundant the moment <code>idx_plays_track_time</code> exists. Redundant indexes are pure cost.</p>\n<hr>\n<h2 id=\"when-an-index-cannot-help\">When an index cannot help</h2>\n<p><strong>You wrapped the column in a function.</strong> Try <em>\"A function call that disables an index\"</em> in Tab 2:</p>\n<pre class=\"code lang-sql\"><code>SELECT count(*) FROM listeners WHERE lower(handle) = 'listener_42';</code></pre>\n<p><code>handle</code> has a unique index. It is not usable, because the index stores <code>handle</code>, not\n<code>lower(handle)</code>. Two fixes: rewrite the predicate to touch the bare column, or build an\nexpression index — <code>CREATE INDEX ON listeners (lower(handle))</code> — which stores the computed value.\nThe second only works for queries spelling the expression <em>exactly</em> the same way.</p>\n<p><strong>A leading wildcard.</strong> <code>LIKE '%kim'</code> cannot use a B-tree; <code>LIKE 'kim%'</code> can. Same reason as the\nphone book.</p>\n<p><strong>The value is not selective enough to be worth it.</strong> This one is subtler than the usual advice\nadmits, and the lab will correct you if you take the folklore at face value.</p>\n<p>Create <code>plays (device)</code> in Tab 3. Four distinct values across a million rows, so <code>device = 'phone'</code>\nmatches about 250,000 — a quarter of the table. The folklore says the planner will ignore an index\nthat unselective. Measure it:</p>\n<pre class=\"code lang-sql\"><code>SELECT avg(ms_played) FROM plays WHERE device = 'phone';\n  without index   30.5 ms    Parallel Seq Scan\n  with index      27.2 ms    Parallel Bitmap Heap Scan   ← used, and barely better</code></pre>\n<p>The planner <em>did</em> use it. It just did not help much, because the query still has to visit the heap\nfor <code>ms_played</code>, and 250,000 heap visits is most of the work either way. <strong>~10% faster, for 7 MB\nand a permanent share of every write.</strong> That is the real lesson: not that unselective indexes are\nrejected, but that they are <em>marginal</em> — they look reasonable in review and quietly are not worth\nit.</p>\n<p>Now change one thing and run it again:</p>\n<pre class=\"code lang-sql\"><code>SELECT count(*) FROM plays WHERE device = 'phone';\n  without index  125 ms    Parallel Seq Scan\n  with index      12 ms    Parallel Index Only Scan     ← 10× faster</code></pre>\n<p>Same index, same selectivity, tenfold win — because <code>count(*)</code> needs no column outside the index,\nso the heap is never touched. A 250,000-row index-only scan beats a million-row heap scan easily.</p>\n<p>So the honest rule is not about selectivity alone:</p>\n<blockquote><p>An index wins when it lets you <strong>avoid work</strong>. High selectivity avoids work by matching few rows.\nCovering the query avoids work by skipping the heap. An unselective index that still requires a\nheap visit per row avoids very little, whatever the plan says.</p></blockquote>\n<p>Rule of thumb for the common case — a query that needs columns outside the index — an index earns\nits place at <strong>well under 10%</strong> selectivity. Above that, measure before you ship, and measure the\nquery you are actually going to run.</p>\n<hr>\n<h2 id=\"the-bill\">The bill</h2>\n<p>Click <strong>Time 20,000 inserts</strong> in Tab 3, add four more indexes, click it again. From this lab:</p>\n<pre class=\"code\"><code>2 indexes on plays    490 ms    24.5 µs/row\n6 indexes on plays    639 ms    32.0 µs/row      +30%</code></pre>\n<p>Every index must be updated by every <code>INSERT</code>, every <code>DELETE</code>, and every <code>UPDATE</code> that touches an\nindexed column. That 30% is permanent, it applies to your write path forever, and no dashboard\nattributes it to the index that caused it.</p>\n<p>So the question is never \"would an index make this query faster?\" — it nearly always would. The\nquestion is <strong>\"is this query worth 6% of my write throughput?\"</strong> Sometimes emphatically yes.\nSometimes it is a query that runs twice a month in a report nobody reads.</p>\n<h3 id=\"finding-the-ones-already-costing-you\">Finding the ones already costing you</h3>\n<p>The index table at the bottom of Tab 3 shows a <strong>scans</strong> column straight from\n<code>pg_stat_user_indexes</code>. Zero scans on a non-unique index means: never used since the server\nstarted, still updated on every write. In production that query is the highest-value thing in this\nlesson:</p>\n<pre class=\"code lang-sql\"><code>SELECT indexrelname, idx_scan, pg_size_pretty(pg_relation_size(indexrelid))\n  FROM pg_stat_user_indexes WHERE idx_scan = 0 ORDER BY pg_relation_size(indexrelid) DESC;</code></pre>\n<p>Check uptime before you drop anything — an index used only by month-end reporting looks unused on\nday three.</p>\n<hr>\n<h2 id=\"building-without-stopping-the-world\">Building without stopping the world</h2>\n<p><code>CREATE INDEX</code> takes a lock that blocks writes for the whole build. On a large production table\nthat is an outage.</p>\n<pre class=\"code lang-sql\"><code>CREATE INDEX CONCURRENTLY idx_plays_track ON plays (track_id);</code></pre>\n<p>Slower, two table passes, cannot run inside a transaction — and does not block writers. Use it in\nproduction, always. It can fail and leave an <code>INVALID</code> index behind, so check\n<code>pg_index.indisvalid</code> afterwards, drop the invalid one, and retry.</p>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why a B-tree serves prefixes and ordering but not suffixes.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Order the columns of a composite index correctly, and justify it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name four reasons an existing index goes unused, and fix each.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;State roughly how selective a predicate must be for an index to win.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Quantify the write cost of an index rather than assuming it is negligible.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Find unused indexes on a live database, and say why uptime matters before dropping them.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say why you would never run a bare <code>CREATE INDEX</code> in production.</li></ul>\n<p><strong>Artifact for this module:</strong> take one real query — from your system, or the artist page in Tab 6\n— and produce a <strong>before/after index record</strong>: the two plans, the two block counts, the index\nsize, the measured write cost, and a short paragraph on why you would or would not ship it. The\nparagraph must argue <em>both</em> sides. An artifact that only shows the speed-up is a sales pitch, not\nan engineering decision.</p>\n<p>Next: <a href=\"#/data-storage/5\">05 · Transactions and isolation</a></p>","words":1521,"minutes":8,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why a B-tree serves prefixes and ordering but not suffixes.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Order the columns of a composite index correctly, and justify it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Name four reasons an existing index goes unused, and fix each.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;State roughly how selective a predicate must be for an index to win.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Quantify the write cost of an index rather than assuming it is negligible.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Find unused indexes on a live database, and say why uptime matters before dropping them.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say why you would never run a bare <code>CREATE INDEX</code> in production.</li></ul>"},{"n":5,"title":"Transactions and isolation","file":"05-transactions-and-isolation.md","slug":"transactions-and-isolation","html":"<p>This is the lesson a book cannot teach you, and the reason this course has a lab.</p>\n<p>An isolation anomaly is not a fact you can memorise. It is a <em>story about interleaving</em>: session\nA does this, session B does that, then A does one more thing and gets an answer that no single\nstatement could ever have produced. You have to watch it happen, in order, twice — once where it\nbreaks and once where it does not.</p>\n<p>Open <strong>Tab 4 · Transactions</strong>. Sessions A and B on that page are two real Postgres backends with\ntheir own PIDs. Nothing is simulated.</p>\n<h2 id=\"acid-briefly-then-the-hard-part\">ACID, briefly, then the hard part</h2>\n<p><strong>Atomicity</strong> — all of it or none of it. <strong>Consistency</strong> — constraints hold at commit.\n<strong>Durability</strong> — a committed write survives a crash. Those three are mostly free; the database\ndoes them and you rarely think about them.</p>\n<p><strong>Isolation</strong> is the one you pay for, and the only one with a dial. It asks: when transactions\nrun concurrently, how much of each other's mess are they allowed to see? Turn the dial up and you\nget correctness; turn it up and you also get contention, aborted transactions and retry logic.\nEvery real system picks a point on that dial, usually by accident.</p>\n<p>Postgres' default is <code>READ COMMITTED</code>. If nobody on your team has chosen otherwise, that is what\nyou are running.</p>\n<hr>\n<h2 id=\"run-the-scenarios-in-this-order\">Run the scenarios in this order</h2>\n<p>Each one uses <strong>the identical script</strong> at every isolation level. Only the level changes — that is\nwhat makes the comparison mean anything.</p>\n<h3 id=\"1-the-dirty-read-that-cannot-happen\">1. The dirty read that cannot happen</h3>\n<p>Run <code>dirty-read</code> at <code>READ UNCOMMITTED</code>. A modifies a row without committing; B reads it and sees\nthe <em>old</em> value.</p>\n<p>Postgres accepts the <code>READ UNCOMMITTED</code> syntax and quietly gives you <code>READ COMMITTED</code> instead. Its\nstorage engine keeps old row versions rather than modifying rows in place, so there is no way to\nshow you an uncommitted one. <strong>You can stop worrying about dirty reads in Postgres.</strong> Worth seeing\nonce so the worry is gone for good.</p>\n<h3 id=\"2-non-repeatable-read\">2. Non-repeatable read</h3>\n<p>Run <code>non-repeatable-read</code> at <code>READ COMMITTED</code>:</p>\n<pre class=\"code\"><code>A: first read  → seconds = 210\nB: update to 999, commit\nA: second read → seconds = 999      ← inside one transaction</code></pre>\n<p>A asked the same question twice in one transaction and got two answers. At <code>READ COMMITTED</code> every\n<em>statement</em> gets a fresh snapshot, so this is working as designed.</p>\n<p>Now switch to <code>REPEATABLE READ</code> and run the identical script:</p>\n<pre class=\"code\"><code>A: first read  → 210\nA: second read → 210      ← B's commit is invisible to A</code></pre>\n<p>One snapshot for the whole <em>transaction</em>. This is the entire difference between the two levels.</p>\n<h3 id=\"3-phantoms\">3. Phantoms</h3>\n<p>Run <code>phantom</code> at <code>READ COMMITTED</code>: A counts featured albums (2), B inserts another and commits, A\ncounts again (3). A row appeared from nowhere.</p>\n<p>At <code>REPEATABLE READ</code> both counts return 2. Note this is <strong>stronger than the SQL standard\nrequires</strong> — the standard permits phantoms at repeatable read, and many databases do. Postgres'\nsnapshot covers ranges as well as rows, so it does not. Portable code should not rely on this;\nyour code running on Postgres can.</p>\n<h3 id=\"4-the-lost-update-the-one-your-code-probably-has\">4. The lost update — the one your code probably has</h3>\n<p>Run <code>lost-update</code> at <code>READ COMMITTED</code>:</p>\n<pre class=\"code\"><code>A reads counter  → 0\nB reads counter  → 0\nA writes 1, commits\nB writes 1, commits\nfinal value = 1        ← two increments, one result</code></pre>\n<p>Nothing errored. No constraint was violated. One user's work silently disappeared. This is the\nread-modify-write race, and it is in more application code than any other bug in this course —\nevery <code>SELECT</code> into a variable, modify in application code, <code>UPDATE</code> back is this shape.</p>\n<p>At <code>REPEATABLE READ</code> the same script raises <strong>40001</strong>, and B must retry.</p>\n<p>The important point: <strong>a higher isolation level is not the fix here — it is the alarm.</strong> The fix\nis to stop splitting the read and the write:</p>\n<pre class=\"code lang-sql\"><code>UPDATE editorial_rules SET min_value = min_value + 1 WHERE rule = 'min_featured_albums';</code></pre>\n<p>One atomic statement. Correct at every isolation level, no retry needed. Or take the row lock\nexplicitly with <code>SELECT ... FOR UPDATE</code> (Lesson 06). Reach for isolation levels when you cannot\nexpress the change as one statement.</p>\n<h3 id=\"5-write-skew-the-one-only-serializable-catches\">5. Write skew — the one only SERIALIZABLE catches</h3>\n<p>This is the scenario worth the whole lesson. Run <code>write-skew</code> at <code>READ COMMITTED</code>:</p>\n<pre class=\"code\"><code>Rule: at least one album must stay featured. Two are featured.\n\nA: count featured → 2. Removing one is safe.\nB: count featured → 2. Removing one is safe.\nA: un-feature b1, commit\nB: un-feature b2, commit\n\nfeatured albums = 0        ← the rule is broken</code></pre>\n<p>Read that again, because it is genuinely unsettling. <strong>Neither transaction did anything wrong.</strong>\nEach checked the invariant and each was correct when it checked. They touched <em>different rows</em>, so\nno lock was ever contended and nothing blocked. <code>REPEATABLE READ</code> does not help either — neither\ntransaction read anything the other wrote, so there is no conflict for snapshot isolation to\ndetect.</p>\n<p>Now run the identical script at <code>SERIALIZABLE</code>:</p>\n<pre class=\"code\"><code>B: COMMIT → ERROR 40001: could not serialize access due to read/write dependencies\nfeatured albums = 1        ← the rule held</code></pre>\n<p><code>SERIALIZABLE</code> in Postgres tracks the <em>dependencies between reads and writes</em>, not just the\nwrites. It notices that A read a set that B wrote and vice versa, and that no serial ordering of\nthe two produces this outcome — so it aborts one.</p>\n<p><strong>Write skew is the anomaly to remember</strong>, because it is the one your instincts do not catch. Any\ntime a transaction reads something to decide whether a write is allowed — inventory, seat\navailability, balance checks, \"is this the last admin?\" — you are exposed to it, and no amount of\ncareful single-transaction reasoning will find it.</p>\n<hr>\n<h2 id=\"choosing-a-level\">Choosing a level</h2>\n<div class=\"tablewrap\"><table><thead><tr><th>Level</th><th>Prevents</th><th>Still allows</th><th>Cost</th></tr></thead><tbody><tr><td><code>READ COMMITTED</code></td><td>dirty reads</td><td>non-repeatable reads, phantoms, lost updates, write skew</td><td>cheapest; Postgres default</td></tr><tr><td><code>REPEATABLE READ</code></td><td>+ non-repeatable reads, phantoms <em>(in Postgres)</em></td><td>lost updates across statements, write skew</td><td>40001 on conflicting updates</td></tr><tr><td><code>SERIALIZABLE</code></td><td>everything above, including write skew</td><td>—</td><td>tracks predicate dependencies; more 40001s under contention</td></tr></tbody></table></div>\n<p>Practical guidance:</p>\n<ul><li>Stay on <code>READ COMMITTED</code> for most work, and fix races with atomic statements, <code>FOR UPDATE</code>, and unique constraints.</li><li>Use <code>SERIALIZABLE</code> for the handful of transactions with a <strong>read-to-decide-then-write</strong> shape. You can mix levels per transaction; it does not have to be a global setting.</li><li>Never use a longer transaction as a substitute for the right level. It just holds locks longer.</li></ul>\n<hr>\n<h2 id=\"40001-is-not-a-failure\">40001 is not a failure</h2>\n<p>The most common mistake after adopting <code>SERIALIZABLE</code> is treating serialization errors as bugs.\nThey are not. <strong>A 40001 is the database telling you it refused to corrupt your data.</strong> The bug is\nnot having a retry loop:</p>\n<pre class=\"code lang-js\"><code>async function withRetry(fn, attempts = 3) {\n  for (let i = 0; i &lt; attempts; i++) {\n    try { return await fn(); }\n    catch (e) {\n      if (e.code !== '40001' &amp;&amp; e.code !== '40P01') throw e;  // not retryable\n      await sleep(2 ** i * 50 + Math.random() * 50);          // back off, jitter\n    }\n  }\n  throw new Error('exhausted retries');\n}</code></pre>\n<p>Three rules: retry the <strong>whole transaction</strong> from the beginning (its snapshot is gone — replaying\none statement is meaningless), back off with jitter so competing transactions do not collide\nagain, and cap the attempts. And keep serializable transactions short: the longer they run, the\nmore they conflict with, and the more retries you generate.</p>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why Postgres has no dirty reads at any isolation level.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Describe the difference between a statement snapshot and a transaction snapshot.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Recognise the read-modify-write shape in code, and fix it without changing isolation level.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain write skew to someone else, including why row locking cannot catch it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose an isolation level per transaction and defend the choice.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Write a correct retry loop, and say why it must retry the whole transaction.</li></ul>\n<p><strong>Artifact for this module:</strong> find a <strong>read-to-decide-then-write</strong> transaction in a real system —\nyours, or one you can describe precisely. Write up: the invariant it assumes, the interleaving that\nbreaks it (concretely — A does this, B does that), and the fix you would ship, choosing between an\natomic statement, an explicit lock, a constraint, and <code>SERIALIZABLE</code> plus retry. Then reproduce\nyour interleaving in this lab to prove the anomaly is real and your fix works. <strong>That reproduction\nis the artifact.</strong> An argument that an anomaly is possible is a hypothesis; a lab transcript is\nevidence.</p>\n<p>Next: <a href=\"#/data-storage/6\">06 · Concurrency and locking</a></p>","words":1480,"minutes":7,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why Postgres has no dirty reads at any isolation level.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Describe the difference between a statement snapshot and a transaction snapshot.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Recognise the read-modify-write shape in code, and fix it without changing isolation level.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain write skew to someone else, including why row locking cannot catch it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose an isolation level per transaction and defend the choice.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Write a correct retry loop, and say why it must retry the whole transaction.</li></ul>"},{"n":6,"title":"Concurrency and locking","file":"06-concurrency-and-locking.md","slug":"concurrency-and-locking","html":"<p>Lesson 05 was about what transactions <em>see</em>. This one is about what they make each other *wait\nfor* — and about the single most common shape of a database incident, which is not a slow query\nat all but a queue behind a lock.</p>\n<h2 id=\"readers-do-not-block-writers\">Readers do not block writers</h2>\n<p>Start here, because it is the fact that makes Postgres pleasant and the fact that misleads people\ninto thinking locks are not their problem.</p>\n<p>Postgres uses MVCC — multi-version concurrency control. An <code>UPDATE</code> does not overwrite a row; it\nwrites a <strong>new version</strong> of it and marks the old one as dead. Readers holding an older snapshot\nkeep reading the old version quite happily.</p>\n<p>The consequence:</p>\n<ul><li>Readers never block readers.</li><li>Readers never block writers.</li><li>Writers never block readers.</li><li><strong>Writers block writers, on the same row.</strong></li></ul>\n<p>Only that last line can make your application wait. Everything in this lesson lives inside it —\nplus the price of all those old row versions, which is Lesson 08.</p>\n<hr>\n<h2 id=\"row-locks-taken-deliberately\">Row locks, taken deliberately</h2>\n<p>Lesson 05's lost update happened because a read and a write were separated in time. <code>SELECT ...\nFOR UPDATE</code> closes the gap by locking the rows as you read them:</p>\n<pre class=\"code lang-sql\"><code>BEGIN;\nSELECT min_value FROM editorial_rules WHERE rule = 'min_featured_albums' FOR UPDATE;\n-- any other transaction asking for this row now waits here\nUPDATE editorial_rules SET min_value = min_value + 1 WHERE rule = 'min_featured_albums';\nCOMMIT;   -- lock released</code></pre>\n<p>Run the <code>lock-wait</code> scenario in Tab 4. Step 2 goes yellow and says <strong>blocked</strong> — B is not\nerroring, not being aborted, just stopped. It stays stopped until A commits, at which point B's\nstatement completes and returns the <em>new</em> value. Final counter: 2. Both increments survived.</p>\n<p>The variants are worth knowing:</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Clause</th><th>Behaviour</th></tr></thead><tbody><tr><td><code>FOR UPDATE</code></td><td>Exclusive. Others wait.</td></tr><tr><td><code>FOR NO KEY UPDATE</code></td><td>Weaker; allows concurrent foreign-key checks. What a plain <code>UPDATE</code> takes.</td></tr><tr><td><code>FOR SHARE</code></td><td>Several readers may hold it; writers wait.</td></tr><tr><td><code>FOR UPDATE NOWAIT</code></td><td>Fail immediately (55P03) instead of waiting.</td></tr><tr><td><code>FOR UPDATE SKIP LOCKED</code></td><td>Skip rows someone else holds. The correct way to build a work queue.</td></tr></tbody></table></div>\n<p><code>SKIP LOCKED</code> deserves the highlight. It is how you let N workers pull from one table without\ncoordination and without any two picking the same row:</p>\n<pre class=\"code lang-sql\"><code>UPDATE jobs SET status = 'running'\n WHERE id IN (SELECT id FROM jobs WHERE status = 'queued'\n               ORDER BY created_at FOR UPDATE SKIP LOCKED LIMIT 1)\nRETURNING *;</code></pre>\n<hr>\n<h2 id=\"deadlock\">Deadlock</h2>\n<p>Run the <code>deadlock</code> scenario and watch it happen:</p>\n<pre class=\"code\"><code>A: BEGIN, lock album b1\nB: BEGIN, lock album b2\nA: wants b2  → blocked (B holds it)\nB: wants b1  → blocked (A holds it)\n\n... 1 second later ...\n\nERROR 40P01: deadlock detected</code></pre>\n<p>Neither can proceed and neither will ever give up, so Postgres runs a cycle detector every\n<code>deadlock_timeout</code> (1 second in this lab's compose file), picks a victim and aborts it. The\nsurvivor continues.</p>\n<p><strong>The cause is not too many locks. It is inconsistent lock ordering.</strong> A took b1 then b2; B took\nb2 then b1. Had both taken them in the same order, the second would simply have waited.</p>\n<p>So the fix is a discipline, not a setting:</p>\n<ol><li><strong>Order your locks.</strong> Pick a rule — ascending primary key is the usual one — and apply it everywhere. <code>ORDER BY id</code> on the <code>SELECT ... FOR UPDATE</code> that grabs multiple rows costs nothing and removes an entire class of incident.</li><li><strong>Take locks late and release them early.</strong> Do the reads and the computation first; lock as close to the commit as you can.</li><li><strong>Keep transactions short.</strong> A transaction that holds locks across an HTTP call to a payment provider will eventually hold them for the provider's timeout.</li><li><strong>Retry 40P01</strong>, exactly like 40001 — same loop from Lesson 05. Deadlock is not a bug you can always design away; it is a condition you must survive.</li></ol>\n<p>Lowering <code>deadlock_timeout</code> does not reduce deadlocks. It only makes you find out sooner.</p>\n<hr>\n<h2 id=\"finding-the-blocker-during-an-incident\">Finding the blocker during an incident</h2>\n<p><strong>Tab 5 · Locks</strong> runs the query worth memorising. Start the deadlock or <code>lock-wait</code> scenario,\nstop on the blocking step, then open the tab:</p>\n<pre class=\"code lang-sql\"><code>SELECT w.pid AS waiting_pid, w.wait_event_type, w.wait_event,\n       b.pid AS blocking_pid, left(b.query, 90) AS blocking_query,\n       now() - w.query_start AS waited\n  FROM pg_stat_activity w\n  LEFT JOIN LATERAL unnest(pg_blocking_pids(w.pid)) AS bp(pid) ON true\n  LEFT JOIN pg_stat_activity b ON b.pid = bp.pid\n WHERE cardinality(pg_blocking_pids(w.pid)) &gt; 0;</code></pre>\n<p><code>pg_blocking_pids()</code> is the function to remember. During an incident it answers the only question\nthat matters — <strong>who is at the head of the queue</strong> — in one hop, instead of you reading\n<code>pg_locks</code> and joining it to itself.</p>\n<p>When you find the culprit: <code>SELECT pg_cancel_backend(pid)</code> cancels its current statement,\n<code>pg_terminate_backend(pid)</code> kills the connection. Try cancel first.</p>\n<hr>\n<h2 id=\"idle-in-transaction-the-quiet-killer\"><code>idle in transaction</code>, the quiet killer</h2>\n<p>The other row that view surfaces. A session that ran <code>BEGIN</code>, did some work, and then went away —\nwaiting on an API call, blocked on application code, or attached to a developer who wandered off.</p>\n<p>It holds every lock it has taken. It also <strong>holds back the oldest snapshot</strong>, which stops VACUUM\nfrom cleaning up dead rows anywhere in the database (Lesson 08). One forgotten transaction can\nbloat tables it never touched.</p>\n<p>Set the guard and stop relying on discipline:</p>\n<pre class=\"code lang-sql\"><code>ALTER SYSTEM SET idle_in_transaction_session_timeout = '30s';</code></pre>\n<p>The general principle behind all of this: <strong>the damage a transaction does is measured by how long\nit holds locks, not by how much work it does.</strong> A transaction that updates one row and then waits\n90 seconds on a third-party API is far more dangerous than one that updates 10,000 rows in 200 ms.\nNever open a transaction across a network call to something you do not control.</p>\n<hr>\n<h2 id=\"table-level-locks-and-migrations\">Table-level locks and migrations</h2>\n<p>Row locks are the ones you meet daily; table locks are the ones that cause outages. <code>ALTER TABLE</code>\ngenerally takes <code>ACCESS EXCLUSIVE</code>, which blocks <em>everything</em>, including reads.</p>\n<p>Modern Postgres has made many operations cheap — adding a nullable column, or a column with a\ndefault, is metadata-only. Others still rewrite the whole table. Two rules that prevent most\nmigration incidents:</p>\n<ul><li><strong>Always set a short <code>lock_timeout</code> before a migration.</strong> Without it, your <code>ALTER TABLE</code> queues behind one long-running query, and every query arriving afterwards queues behind <em>your</em> <code>ACCESS EXCLUSIVE</code> request. A five-second-lock migration takes the site down for ten minutes.<pre class=\"code\"><code>SET lock_timeout = '3s';\nALTER TABLE plays ADD COLUMN source text;</code></pre> Failing fast and retrying is strictly better than blocking the world.</li><li><strong>Add constraints in two steps.</strong> <code>ADD CONSTRAINT ... NOT VALID</code> takes a brief lock and applies to new rows; <code>VALIDATE CONSTRAINT</code> then scans the existing rows without blocking writes.</li></ul>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;State which of the four reader/writer combinations can block, and why MVCC frees the others.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose between <code>FOR UPDATE</code>, <code>NOWAIT</code> and <code>SKIP LOCKED</code>, and build a work queue with the last.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain what actually causes deadlocks and give the structural fix.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Find the blocking PID on a live database and decide between cancel and terminate.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain how one <code>idle in transaction</code> session bloats tables it never touched.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say why every migration should set <code>lock_timeout</code> first.</li></ul>\n<p><strong>Artifact for this module:</strong> cause a deadlock in the lab, capture the full transcript from both\nsessions plus the wait edge from Tab 5. Then write the <strong>lock-ordering rule</strong> that would have\nprevented it, stated precisely enough that another engineer could apply it — and find one place in\na real codebase where that rule is currently violated. The rule is the deliverable; the deadlock\nis just proof you understand what you are ruling out.</p>\n<p>Next: <a href=\"#/data-storage/7\">07 · Connections, pooling, and the N+1 you already met</a></p>","words":1334,"minutes":7,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;State which of the four reader/writer combinations can block, and why MVCC frees the others.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose between <code>FOR UPDATE</code>, <code>NOWAIT</code> and <code>SKIP LOCKED</code>, and build a work queue with the last.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain what actually causes deadlocks and give the structural fix.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Find the blocking PID on a live database and decide between cancel and terminate.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain how one <code>idle in transaction</code> session bloats tables it never touched.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say why every migration should set <code>lock_timeout</code> first.</li></ul>"},{"n":7,"title":"Connections, pooling, and the N+1 you already met","file":"07-connections-and-n-plus-1.md","slug":"connections-pooling-and-the-n-1-you-already-met","html":"<p>In the API Design course, the compare chart showed the naive REST implementation costing <strong>four\nHTTP round trips</strong> to render one artist page, and you learned to see that as the cost. It was\nhalf the bill. Each of those four requests also ran queries, and nobody counted them.</p>\n<p>This lesson counts them, and then looks at the layer underneath — the connections those queries\ntravel on, which is where a surprising amount of production latency hides.</p>\n<h2 id=\"part-1-the-n-1-priced\">Part 1 · The N+1, priced</h2>\n<p>Open <strong>Tab 6 · N+1</strong> and run it. Three implementations of the same artist page, against the real\ndatabase:</p>\n<pre class=\"code\"><code>N+1 (a query per album)          6 queries    16.1 ms\nOne join, nested in app code     1 query       4.1 ms\nOne query, shaped in SQL         1 query       3.7 ms</code></pre>\n<p>The N+1 version is the one that looks like good code:</p>\n<pre class=\"code lang-js\"><code>const artist = await db.one('SELECT * FROM artists WHERE id = $1', [id]);\nconst albums = await db.many('SELECT * FROM albums WHERE artist_id = $1', [id]);\nfor (const album of albums) {\n  album.tracks = await db.many('SELECT * FROM tracks WHERE album_id = $1', [album.id]);\n}</code></pre>\n<p>Nothing is wrong with any individual line. That is the whole problem: <strong>N+1 is invisible at the\nstatement level and only appears when you count statements.</strong> Four albums today is six queries.\nAn artist with sixty albums is sixty-two, and the loop that produced them still looks fine in\nreview.</p>\n<p>This is also exactly what an ORM does by default when you traverse a relation in a loop — the\nlazy-loading feature is the N+1 generator. Most ORMs offer an eager-load or <code>include</code> option; it\nexists because of this lesson.</p>\n<h3 id=\"reading-the-three-rows-honestly\">Reading the three rows honestly</h3>\n<p><strong>Fewer queries is not automatically better.</strong> The third row is the fastest and it buys that with\n<code>jsonb_agg</code> and a lateral join — SQL that a colleague will have to reverse-engineer in six months.\nThe second row is nearly as fast, plain to read, and repeats the artist's columns on every\nreturned row.</p>\n<p>The engineering answer is usually the middle one, and the reason to know all three is to\nrecognise which problem you have:</p>\n<ul><li><strong>Latency-bound</strong> (many round trips to a database over a network) → collapse to one query.</li><li><strong>Bandwidth-bound</strong> (one query returning hugely redundant columns) → shape it in SQL.</li><li><strong>Neither</strong> → leave the readable version alone.</li></ul>\n<h3 id=\"the-index-interaction\">The index interaction</h3>\n<p>Run the tab before adding indexes, then add <code>tracks (album_id)</code> in Tab 3 and run it again. The\nN+1 gets faster — but it is <em>still</em> an N+1. Indexing reduces the cost of each query; it does not\nreduce the count. Round trips and per-query cost are independent problems, and fixing one does not\nfix the other.</p>\n<hr>\n<h2 id=\"part-2-connections-are-not-free\">Part 2 · Connections are not free</h2>\n<p>A Postgres connection is not a socket. It is a <strong>forked backend process</strong> on the server, with its\nown memory. Measure it in <strong>Tab 7 · Pool &amp; cost</strong>:</p>\n<pre class=\"code\"><code>new connection    11.62 ms\npooled             0.91 ms      12.7× cheaper</code></pre>\n<p>Twelve milliseconds, on a database running on the same machine with no network in the way. Add a\nnetwork and TLS and it is worse. If your application opens a connection per request, you are\npaying that on every request — and if your traffic doubles, you are also forking twice as many\nprocesses on a server that has a hard <code>max_connections</code> limit.</p>\n<p>A pool keeps connections open and lends them out. \"Connect per request\" becomes \"borrow per\nrequest,\" and the fork cost is amortised to nearly nothing.</p>\n<hr>\n<h2 id=\"part-3-the-pool-has-a-floor-and-it-queues-silently\">Part 3 · The pool has a floor, and it queues silently</h2>\n<p>This is the part that catches people, and it is why this tab exists.</p>\n<p>The lab's pool holds 8 connections. Run it at concurrency 8, then at 40:</p>\n<pre class=\"code\"><code>concurrency 8     total  67 ms    queue wait p95   14.3 ms\nconcurrency 40    total 267 ms    queue wait p95  213.4 ms</code></pre>\n<p>Each request did 50 ms of work in both runs. Nothing about the database got slower. But at\nconcurrency 40, thirty-two requests could not start until a connection freed up, and the p95\nrequest spent <strong>213 ms waiting to begin</strong>.</p>\n<p>Now think about where that time shows up. Not in <code>pg_stat_statements</code> — from Postgres' point of\nview every query took 50 ms. Not in database CPU, which is idle. It appears only in your\napplication's response times, as latency with no visible cause.</p>\n<p><strong>Slow endpoint, fast queries, idle database CPU</strong> is the signature. When you see it, look at pool\nwait time before you look at anything else. If your pool does not expose that metric, instrument\nit — the time between \"I want a connection\" and \"I have one\" is one of the highest-value numbers\nin a web application, and most teams do not measure it.</p>\n<p>The arithmetic is simple enough to do in your head: with a pool of <code>P</code> and requests taking <code>D</code>,\n<code>N</code> concurrent requests take about <code>ceil(N/P) × D</code>. The lab prints its prediction next to the\nmeasurement so you can watch the model hold.</p>\n<h3 id=\"sizing\">Sizing</h3>\n<p>The instinct is to raise the pool until the queue disappears. That moves the queue rather than\nremoving it: past the point where the server has spare cores and disk, more concurrent queries\njust means each one runs slower.</p>\n<ul><li>Start near <strong>cores × 2</strong>, then measure under real load.</li><li>Remember pools are <strong>per process</strong>. Ten pods with a pool of 20 is 200 connections. Compare that with <code>max_connections</code> (shown in the tab) <em>before</em> you scale out.</li><li>If you genuinely need hundreds of application instances, put <strong>PgBouncer</strong> in front in transaction-pooling mode. It multiplexes many client connections onto few server ones — at the cost of session state: no session-level <code>SET</code>, no advisory locks, prepared statements need care.</li><li><strong>Fix the slow queries first.</strong> A pool exhausted by queries that should take 2 ms and take 200 is not a pool-sizing problem.</li></ul>\n<h3 id=\"the-one-that-takes-the-whole-site-down\">The one that takes the whole site down</h3>\n<p>A connection held by an <code>idle in transaction</code> session is out of the pool <em>and</em> holding locks <em>and</em>\npinning the oldest snapshot. Combine that with the previous lesson's advice and you get the rule:</p>\n<blockquote><p><strong>Never hold a database connection across a call to something you do not control.</strong></p></blockquote>\n<p>Fetch what you need, close the transaction, <em>then</em> call the payment provider. A third-party\ntimeout should cost you one request, not your entire pool.</p>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Spot the N+1 shape in ordinary-looking code, and count the statements a request issues.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose between N+1, one join, and shaping in SQL — and justify not always picking the fastest.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why indexing an N+1 does not fix the N+1.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what a connection costs and why, in terms of what the server actually creates.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Predict the latency of <code>N</code> concurrent requests against a pool of <code>P</code>.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Recognise the \"slow endpoint, fast queries, idle CPU\" signature and know where to look.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Size a pool, accounting for every process that has one.</li></ul>\n<p><strong>Artifact for this module:</strong> a <strong>pool sizing note</strong> for a real service. State its instance count,\npool size per instance, the resulting total, and the server's <code>max_connections</code> — then say whether\nthose numbers are safe and what happens during a deploy when old and new instances overlap. Add\nthe p95 pool wait time if you can measure it, or \"not currently measured\" if you cannot; naming\nthe gap is a legitimate finding, and usually the most valuable line in the note.</p>\n<p>Next: <a href=\"#/data-storage/8\">08 · Scaling one node</a></p>","words":1289,"minutes":6,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Spot the N+1 shape in ordinary-looking code, and count the statements a request issues.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Choose between N+1, one join, and shaping in SQL — and justify not always picking the fastest.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why indexing an N+1 does not fix the N+1.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what a connection costs and why, in terms of what the server actually creates.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Predict the latency of <code>N</code> concurrent requests against a pool of <code>P</code>.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Recognise the \"slow endpoint, fast queries, idle CPU\" signature and know where to look.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Size a pool, accounting for every process that has one.</li></ul>"},{"n":8,"title":"Scaling one node","file":"08-scaling-one-node.md","slug":"scaling-one-node","html":"<p>Before you shard, before you add read replicas, before you move to something with \"distributed\"\nin its marketing: one Postgres node on decent hardware handles far more than most teams assume.\nTens of thousands of transactions per second, tables in the hundreds of millions of rows. Most\nsystems that \"outgrew Postgres\" outgrew an unindexed query, a bloated table, or a pool of 200\nconnections fighting over eight cores.</p>\n<p>This lesson covers what you do to one node before distributing anything — and the maintenance\nthat MVCC quietly requires.</p>\n<h2 id=\"dead-rows-bloat-and-why-vacuum-exists\">Dead rows, bloat, and why VACUUM exists</h2>\n<p>From Lesson 06: an <code>UPDATE</code> writes a new row version and marks the old one dead. A <code>DELETE</code> just\nmarks it dead. Nothing is reclaimed immediately, because some older transaction might still need\nto see it.</p>\n<p>So every update and delete leaves litter. <strong>VACUUM</strong> is the cleaner. It:</p>\n<ul><li>marks dead row space reusable (plain <code>VACUUM</code>) — it does <em>not</em> return disk to the OS,</li><li>updates the <strong>visibility map</strong>, which is what makes Index Only Scans possible,</li><li>refreshes planner statistics (<code>ANALYZE</code>),</li><li>advances the transaction-ID freeze horizon.</li></ul>\n<p>Autovacuum runs this for you. It is usually fine, and the two ways it stops being fine are worth\nknowing:</p>\n<p><strong>Bloat.</strong> A table where dead rows accumulate faster than autovacuum clears them grows on disk\nwhile holding the same live data. Sequential scans get slower in proportion to the <em>physical</em> size,\nnot the useful size. Heavy-update tables need more aggressive settings than the defaults:</p>\n<pre class=\"code lang-sql\"><code>ALTER TABLE plays SET (autovacuum_vacuum_scale_factor = 0.02);  -- default 0.2</code></pre>\n<p>The default only triggers a vacuum after 20% of the table is dead, which on a large table is an\nenormous amount of garbage.</p>\n<p><strong>Blocked cleanup.</strong> VACUUM can only remove versions older than the <strong>oldest running snapshot</strong>.\nOne long-running transaction — or one <code>idle in transaction</code> session from Lesson 06 — pins that\nhorizon and stops cleanup <em>across the whole database</em>. This is the mechanism by which a forgotten\n<code>BEGIN</code> in one service bloats tables it never touched.</p>\n<pre class=\"code lang-sql\"><code>SELECT relname, n_dead_tup, last_autovacuum FROM pg_stat_user_tables ORDER BY n_dead_tup DESC;\nSELECT max(age(backend_xmin)) FROM pg_stat_activity;   -- how far behind the oldest snapshot is</code></pre>\n<p>To actually return disk to the OS you need <code>VACUUM FULL</code> (rewrites the table, takes <code>ACCESS\nEXCLUSIVE</code>, an outage on a big table) or <code>pg_repack</code> (does it without the exclusive lock — use\nthis one).</p>\n<hr>\n<h2 id=\"partitioning\">Partitioning</h2>\n<p>The <code>plays</code> table in this lab grows forever. Time-series data always does. Partitioning splits it\ninto child tables under one parent:</p>\n<pre class=\"code lang-sql\"><code>CREATE TABLE plays (...) PARTITION BY RANGE (played_at);\nCREATE TABLE plays_2026_09 PARTITION OF plays\n  FOR VALUES FROM ('2026-09-01') TO ('2026-10-01');</code></pre>\n<p>What it actually buys:</p>\n<ul><li><strong>Partition pruning.</strong> A query with <code>WHERE played_at &gt; now() - interval '7 days'</code> reads one or two partitions instead of all of them. The planner does this at plan time.</li><li><strong>Cheap deletion.</strong> <code>DROP TABLE plays_2024_01</code> removes a month instantly. <code>DELETE FROM plays WHERE played_at &lt; ...</code> on a large table is slow, produces enormous bloat, and forces a vacuum. <strong>This is usually the real reason to partition.</strong></li><li><strong>Smaller indexes.</strong> Per-partition indexes on a working set that fits in memory.</li></ul>\n<p>What it does not buy: it is not a substitute for indexing, and it does not help queries that have\nno predicate on the partition key. A query filtering only on <code>track_id</code> still touches every\npartition — and now pays planning overhead for each.</p>\n<p>Rules: partition on the column your queries filter and your retention policy deletes by (almost\nalways time); keep partitions in the dozens-to-low-hundreds; automate creation ahead of time,\nbecause a missing partition is a failed insert at midnight.</p>\n<hr>\n<h2 id=\"configuration-that-actually-matters\">Configuration that actually matters</h2>\n<p>Most Postgres tuning advice is noise. These four are not:</p>\n<div class=\"tablewrap\"><table><thead><tr><th>Setting</th><th>Rough starting point</th><th>Why</th></tr></thead><tbody><tr><td><code>shared_buffers</code></td><td>25% of RAM</td><td>Postgres' own block cache. The rest of RAM is still used by the OS page cache, which is why 25% rather than 80%.</td></tr><tr><td><code>work_mem</code></td><td>4–64 MB</td><td>Memory <strong>per sort or hash node, per query</strong>. Too low sends sorts to disk (<code>external merge</code> in your plans); too high times concurrency equals an OOM.</td></tr><tr><td><code>effective_cache_size</code></td><td>50–75% of RAM</td><td>Not an allocation — a <em>hint</em> about how much caching exists. Too low makes the planner distrust indexes.</td></tr><tr><td><code>max_connections</code></td><td>low; use a pool</td><td>Each one is a process. See Lesson 07.</td></tr></tbody></table></div>\n<p><code>work_mem</code> is the one worth watching in plans. When you see <code>Sort Method: external merge Disk:\n48MB</code>, that sort spilled — raising <code>work_mem</code> for that query (you can <code>SET</code> it per transaction)\nturns it into <code>quicksort Memory</code>.</p>\n<p>Set <code>log_min_duration_statement</code> to something like <code>500ms</code> in production. Logging every slow query\ncosts almost nothing and is the highest-value observability change available. This lab sets it to\n<code>0</code> — log everything — which is deliberately excessive and useful for learning: run\n<code>docker compose logs -f db</code> while clicking around the lab and watch every statement appear with\nits duration.</p>\n<hr>\n<h2 id=\"read-replicas\">Read replicas</h2>\n<p>Streaming replication ships the write-ahead log to a standby, which replays it. The standby can\nserve reads.</p>\n<p>This is the first genuinely distributed thing in this lesson, and it comes with the trade-off\nLesson 01 promised: <strong>replication lag</strong>. A replica is behind the primary by anywhere from\nmilliseconds to minutes. Write a row, immediately read it from a replica, and it may not be there.</p>\n<p>The failure mode is always the same: user submits a form, is redirected to a page that reads from\na replica, and their change has vanished. Fixes:</p>\n<ul><li>Route reads that must be current back to the primary — \"read your own writes.\"</li><li>Use <code>pg_current_wal_lsn()</code> / <code>pg_last_wal_replay_lsn()</code> to wait for a replica to catch up to a specific position when it matters.</li><li>Accept staleness where it genuinely does not matter (analytics, reports, search indexes) — which is most read traffic, and why replicas work at all.</li></ul>\n<p>Replicas scale reads. <strong>They do nothing for write throughput</strong> — every replica replays every write.</p>\n<hr>\n<h2 id=\"sharding-and-why-it-is-last\">Sharding, and why it is last</h2>\n<p>Splitting data across independent databases by key. It is the only thing that scales writes\nhorizontally, and it is a large one-way door:</p>\n<ul><li>Cross-shard joins and transactions get hard or impossible.</li><li>Rebalancing is a project.</li><li>Every query needs the shard key, or it fans out to all shards.</li><li>Your operational surface multiplies.</li></ul>\n<p>Do all of this first, in order: fix the queries and indexes, size the pool, deal with bloat,\npartition the big table, move heavy reads to replicas, buy a bigger machine (this is genuinely\nunderrated — vertical scaling is cheap compared to an engineer-year), archive cold data. Only then\nshard.</p>\n<p>The systems that shard successfully usually have a natural boundary already — per-tenant, per-\nregion — where cross-shard queries are rare by construction. If you do not have one, you are\nchoosing to build a distributed database by hand.</p>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why MVCC makes VACUUM necessary, and what plain VACUUM does and does not reclaim.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Describe how one long transaction bloats tables it never touched.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what partitioning buys, and name the case where it is mainly about deletion.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain <code>shared_buffers</code>, <code>work_mem</code> and <code>effective_cache_size</code> and the failure mode of each.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Recognise a spilled sort in a plan and fix it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain read-your-own-writes and how replication lag breaks it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Give the ordered list of things to do before sharding.</li></ul>\n<p><strong>Artifact for this module:</strong> a <strong>maintenance and growth plan</strong> for one real table. Include:\ncurrent size and growth rate, whether it is update-heavy and therefore bloat-prone, its retention\npolicy (and whether that is currently enforced by anything), whether it should be partitioned and\non what key, and the three settings you would check first if it became slow. If the retention\npolicy is \"we have never deleted anything,\" write that down — it is the finding.</p>\n<p>Next: <a href=\"#/data-storage/9\">09 · When relational is the wrong answer</a></p>","words":1359,"minutes":7,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why MVCC makes VACUUM necessary, and what plain VACUUM does and does not reclaim.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Describe how one long transaction bloats tables it never touched.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what partitioning buys, and name the case where it is mainly about deletion.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain <code>shared_buffers</code>, <code>work_mem</code> and <code>effective_cache_size</code> and the failure mode of each.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Recognise a spilled sort in a plan and fix it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain read-your-own-writes and how replication lag breaks it.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Give the ordered list of things to do before sharding.</li></ul>"},{"n":9,"title":"When relational is the wrong answer","file":"09-when-relational-is-wrong.md","slug":"when-relational-is-the-wrong-answer","html":"<p>Eight lessons of Postgres have earned this one a fair hearing. Relational databases are the right\ndefault for most systems — the model is honest, the constraints are enforceable, the query\nlanguage answers questions nobody anticipated, and the operational knowledge is everywhere. But\n\"right default\" is not \"right always,\" and the failure mode of this course would be leaving you\nunable to recognise the cases where something else fits better.</p>\n<p>This lesson is the decision, and the decision record that closes the course.</p>\n<h2 id=\"start-with-the-access-pattern-not-the-technology\">Start with the access pattern, not the technology</h2>\n<p>The question is never \"SQL or NoSQL.\" It is:</p>\n<blockquote><p><strong>What does this data need to do, and what am I willing to give up to make it do that?</strong></p></blockquote>\n<p>Every store below is a set of things given up in exchange for something. Name both halves and the\nchoice usually makes itself.</p>\n<h3 id=\"key-value-redis-dynamodb-memcached\">Key-value (Redis, DynamoDB, Memcached)</h3>\n<p>Get and put by key, extremely fast. Gives up: querying by anything other than the key, joins, and\noften durability guarantees.</p>\n<p>Right for: sessions, caches, rate limiters, feature flags, leaderboards. Wrong for: anything you\nwill later want to query a different way — and you usually will.</p>\n<p>The most common real use is a <strong>cache in front of Postgres</strong>, which is the next course in this\nseries. Note what a cache actually gives up: correctness at the edges. Invalidation is the hard\npart, and a cache is a second copy of the truth that can disagree with the first.</p>\n<h3 id=\"document-mongodb-dynamodb-or-postgres-jsonb\">Document (MongoDB, DynamoDB, or Postgres <code>jsonb</code>)</h3>\n<p>Store a nested document, retrieve it whole. Gives up: enforced structure, and cheap querying\nacross documents.</p>\n<p>Right for: genuinely schema-variable data — per-customer form definitions, event payloads from\nsources you do not control, content with heterogeneous shapes. Wrong for: data with real relations,\nwhich is most data. The \"no schema\" appeal usually means the schema moved into your application\ncode, unenforced, in several places at once.</p>\n<p><strong>Important</strong>: Postgres has <code>jsonb</code>, with indexing and rich operators. A <code>jsonb</code> column on a\nrelational table covers a large fraction of \"we need document storage\" — structured columns for the\nfields you query and constrain, <code>jsonb</code> for the genuinely variable part. Reach for a separate\ndocument database only when that stops working, not before.</p>\n<h3 id=\"columnar-analytical-clickhouse-bigquery-snowflake-duckdb\">Columnar / analytical (ClickHouse, BigQuery, Snowflake, DuckDB)</h3>\n<p>Store by column rather than by row, so scanning one column across a billion rows reads only that\ncolumn. Gives up: efficient single-row reads and updates, and often transactions.</p>\n<p>Right for: analytics — <code>SELECT device, count(*) FROM plays GROUP BY device</code> over a billion rows.\nWrong for: serving your application. This is the clearest case in the list, because it is a\ngenuinely different physical layout, and it is why \"run analytics on a read replica\" only works up\nto a point. When your reporting queries are scanning the entire <code>plays</code> table, you do not need a\nbigger Postgres; you need a column store fed from it.</p>\n<h3 id=\"search-elasticsearch-opensearch-postgres-full-text\">Search (Elasticsearch, OpenSearch, Postgres full-text)</h3>\n<p>Inverted indexes, relevance ranking, fuzzy matching. Gives up: being a source of truth — it is a\nderived index, and rebuildable.</p>\n<p>Postgres' built-in full-text search with a GIN index handles more than people expect. Move to a\ndedicated engine when you need relevance tuning, faceting and typo tolerance, and keep Postgres as\nthe system of record.</p>\n<h3 id=\"graph-neo4j-or-recursive-ctes-in-postgres\">Graph (Neo4j, or recursive CTEs in Postgres)</h3>\n<p>Traversals of arbitrary depth. Gives up: general-purpose everything else.</p>\n<p>Right when traversal <em>is</em> the product — social graphs, fraud rings, dependency resolution. For\nbounded depth, Postgres recursive CTEs are usually enough. \"We have relationships in our data\" is\nnot a reason; every relational database is named after relations.</p>\n<h3 id=\"time-series-timescaledb-influxdb-prometheus\">Time series (TimescaleDB, InfluxDB, Prometheus)</h3>\n<p>Append-heavy, time-ordered, queried in windows, downsampled as it ages. <code>plays</code> in this lab is\nexactly this shape.</p>\n<p>Timescale is a Postgres extension, which makes it the low-risk option: partitioning, compression\nand time-bucketing without leaving SQL or your existing tooling.</p>\n<hr>\n<h2 id=\"polyglot-persistence-and-its-bill\">Polyglot persistence, and its bill</h2>\n<p>Multiple stores, each suited to its job, is a legitimate architecture. It also multiplies:\noperational surface, failure modes, on-call knowledge, and — the one people underestimate — <strong>the\nnumber of places the same fact lives.</strong></p>\n<p>Two copies of a fact will disagree. Not might: will. So every additional store needs an answer to:\nwhich one is authoritative, how does the other get updated, what happens when that pipeline fails,\nand how do you detect drift.</p>\n<p>The healthy pattern is a clear <strong>system of record</strong> plus derived stores that can be rebuilt from\nit. A search index is a projection of Postgres. A cache is a projection. An analytics warehouse is\na projection. If you cannot say which store is authoritative for a fact, that is a design bug, and\nit will surface as a support ticket about numbers that do not match.</p>\n<hr>\n<h2 id=\"the-honest-defaults\">The honest defaults</h2>\n<p>If you want a heuristic rather than a framework:</p>\n<ol><li><strong>Start with Postgres.</strong> It does relational, JSON, full-text, geospatial, time-series (with Timescale), and queues (<code>SKIP LOCKED</code>) well enough that one node covers most systems for years.</li><li><strong>Add a cache</strong> when you have measured a read path that is hot and tolerant of staleness.</li><li><strong>Add a column store</strong> when analytical queries start hurting the transactional workload.</li><li><strong>Add a search engine</strong> when relevance becomes a feature rather than a filter.</li><li><strong>Add anything else</strong> when you can name the access pattern Postgres cannot serve and the thing you are giving up to serve it.</li></ol>\n<p>The order matters: each step adds a copy of the truth, and copies are the expensive part.</p>\n<hr>\n<h2 id=\"common-failure-modes-named\">Common failure modes, named</h2>\n<ul><li><strong>The document database for relational data</strong> — chosen for schema flexibility, discovered later to need joins, now doing them in application code.</li><li><strong>Analytics on the production primary</strong> — one dashboard query scanning <code>plays</code> while checkouts wait behind it.</li><li><strong>The cache with no invalidation story</strong> — fast, wrong, and impossible to debug because the two copies disagree only sometimes.</li><li><strong>The queue that is a table but was never designed as one</strong> — polling with <code>SELECT ... LIMIT 1</code>, two workers claiming one job, no <code>SKIP LOCKED</code>.</li><li><strong>Sharding at 10 GB</strong> — a distributed system adopted to solve a missing index.</li><li><strong>The replica read that races the write</strong> — user updates their profile, sees the old one, files a bug you cannot reproduce.</li><li><strong>Postgres declared \"outgrown\"</strong> without anyone reading a query plan.</li></ul>\n<hr>\n<h2 id=\"what-you-should-now-be-able-to-do\">What you should now be able to do</h2>\n<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Frame a storage choice as an access pattern plus an explicit trade, not a technology preference.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what each of the six store families gives up, not just what it gives.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Argue for <code>jsonb</code> in Postgres over a separate document store, and name where that stops.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why polyglot persistence's real cost is duplicated facts.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Identify the system of record for every fact in a design.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Give the ordered list of what to do before adding a second store.</li></ul>\n<hr>\n<h2 id=\"artifact-for-this-module-and-for-the-course\">Artifact for this module — and for the course</h2>\n<p>A <strong>one-page storage decision record</strong> for a real project. It should contain:</p>\n<ul><li><strong>Context</strong> — the system, its scale now and its expected scale, the team that will operate it.</li><li><strong>The access patterns</strong> — the specific reads and writes that matter, with rough volumes and latency requirements. Be concrete: \"top 10 tracks for an artist over 90 days, ~200 rps, p95 under 100 ms\" beats \"fast analytics.\"</li><li><strong>The options considered</strong> — including \"one Postgres node, indexed properly,\" which must always be on the list.</li><li><strong>Measurements</strong> — real numbers from this lab or from your own system. Query plans, block counts, pool wait times, isolation-level behaviour. <strong>A decision record with no numbers is an opinion.</strong></li><li><strong>The decision</strong>, and the trade you are accepting — what you are giving up, named explicitly.</li><li><strong>The operational cost</strong> — what breaks at 3am, who knows how to fix it, what you now have to monitor.</li><li><strong>What would change your mind</strong> — the specific measurement or growth threshold that would make you revisit this. A decision with no trigger for revisiting is a decision nobody will revisit.</li></ul>\n<p>That last section is the one that separates an engineering document from a justification. It is\nalso the one that makes the record useful in a year, when the person reading it is you, and the\nnumbers have changed.</p>\n<hr>\n<p>Back to the <a href=\"#/data-storage\">course plan</a>.</p>","words":1423,"minutes":7,"objectives":"<ul class=\"checklist\"><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Frame a storage choice as an access pattern plus an explicit trade, not a technology preference.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Say what each of the six store families gives up, not just what it gives.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Argue for <code>jsonb</code> in Postgres over a separate document store, and name where that stops.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Explain why polyglot persistence's real cost is duplicated facts.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Identify the system of record for every fact in a design.</li><li>&lt;span class=\"box\"&gt;&lt;/span&gt;Give the ordered list of what to do before adding a second store.</li></ul>"}]}]};
